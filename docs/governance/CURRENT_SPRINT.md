# Current Sprint

**As of:** 2026-08-24
**Replace, don't accumulate:** this file reflects only what's active *right now*. When a sprint ends, move its outcome into [[changelog]] and overwrite this file for the next one — do not let this turn into a history log (that's what the changelog is for). **This file and [[project-state]] had gone stale for five days (last genuinely updated 2026-08-17/18) across ten-plus real, shipped workstreams** — corrected in this pass per `teracom-ai-docs`' `ORGANISATIONAL_MEMORY_STRATEGY_V1.md`; see that document's own diagnosis of why a "replace, don't accumulate" file drifted anyway (§3), and its recommendation (§5.2) to make this update a hard step in every workstream's own closing checklist going forward, not a norm that can be silently skipped.

---

## Where things stand

Every package/workstream through Package Q, Wave 1, and Wave 2 (5 further backend workstreams: RAG-to-Chat Wiring, Deployment & Health Readiness, Onboarding & Trust Surface Clarity, Package GOV1 Governance Cascade, Commerce-to-Licensing Lifecycle Automation) is real, committed, and pushed on both `teracom-ai-backend` and `teracom-ai-frontend` — full package-by-package detail lives in [[project-state]] §2/§3, not repeated here. Since then, in roughly chronological order: **Package OPS1** (production systemd service/runbook) and **UX2** (branding consolidation) shipped 2026-08-18; the **Human Authority Model & Customer Readiness Review Implementation** wave (real 5-tier role hierarchy, Read Only Tier Enforcement, User Management endpoints, a real cross-organisation task-assignment leak found and fixed, an unattended scheduler, Department Head second-pass routing, and completion of every previously-missing/partial frontend surface) closed every actionable finding in `TERACOM_CUSTOMER_READINESS_REVIEW_V1.md` on 2026-08-20–21; two **Product Experience/Productisation** copy-and-UX batches followed the same day; **Website/Application Separation** extracted the marketing/commerce site into its own `teracom-solutions-website` repository (Phases 1–4 complete, Phase 5 DNS cutover still pending) on 2026-08-21; the full 9-step **Organisation Workflow Wizard** (Wizard Framework V1–V5), **Settings & Security V1** (MFA, sessions, login history, org security policy), **Visual Identity V1 + Image Pack V1**, and the new documentation-only **`teracom-ai-platform`** architecture repository all shipped 2026-08-22; and **UI Review Sprint V1** plus **Product Experience & Platform Maturity Review V1** (its Priority 1/2 findings implemented: nav entry points, banner de-duplication, Federation's nav placement, Login's palette groundwork) landed 2026-08-23, alongside a from-scratch, no-theatre **Platform Security Review** and a **Flowise/n8n strategy re-validation**. [[project-state]] carries the full evidence trail (commits, test counts, live-verification detail) for every item above; this file tracks only what's still open.

**Most current, still-fresh work as of today**: `PRODUCT_EXPERIENCE_REVIEW_V1.md`'s own prioritised UX roadmap (items 1–3 implemented, items 4–11 open — folded into Active Work below) and `PLATFORM_SECURITY_REVIEW_V1.md`'s effort-ranked security recommendations (also folded in below). Both are review-only documents — no further code changed beyond what's already listed as shipped above.

**AUTONOMOUS_EXECUTION_V1 / AUTONOMOUS_ORGANISATION_V1 / AUTONOMOUS_PROJECTS_V1 have since shipped across five same-day rounds** (2026-08-23) — the first capability letting a worker's own turn read/write real files inside a disposable sandbox and be checked by a real, deterministic `pytest` run, rather than trusted on a chat response alone: (1) execution core + verification gate; (2) the "QA returns defects, Developer receives fixes" retry/feedback loop; (3) a real task dependency graph, system-triggered worker handoffs, a worker-callable `query_memory` tool, the `POST /projects/plan` Project-container entry point, and GUI integration; (4) **Project made the primary operating unit** — `create_autonomous_project()` now actually chains its own decomposed subtasks via `depends_on_task_id` and hands off the final one to QA (round 3's mechanisms existed but the primary "Human → Project" entry point never exercised either one until now); `Project`'s lifecycle is a real, bidirectional state machine (`active` ↔ `blocked` ↔ `completed`, opt-in to `auto_complete` projects, no migration needed); a new `GET /projects/{id}/status` gives real status tracking; project completion now writes real memory for every contributing worker; (5) **the last two items on `AUTONOMOUS_ORGANISATION_V1.md`'s own §9 build sequence** — Web Developer/QA/Documentation worker-role templates seeded (`workerTemplates.js`, 11 → 14), a one-hop QA→Documentation handoff chain added to `_maybe_create_handoff()`, and the unattended scheduler upgraded from the old unverified `execute_chain()` call to the real `run_task_execution()` engine plus a dependency-graph-aware task selection and a new, opt-in (`SCHEDULER_AUTOSTART`, default off) in-process background daemon — `main.py`'s first-ever startup/shutdown hooks. Six real bugs found and fixed along the way, not papered over — see [[project-state]] §3 for the full detail on each, including round 4's own (a project could read "completed" with a real pending handoff task sitting right next to it — an ordering bug, caught live, fixed by creating the handoff before the completion check runs) and round 5's real-Ollama scheduler test rewrite (asserting the mechanism ran for real rather than that the local model's fix always passes). Backend: 45 new tests across the five rounds (`test_task_execution.py` 21, `test_autonomous_organisation.py` 13, `test_autonomous_projects.py` 10, `test_scheduler.py` +1), full suite 496/496 (including real Ollama runs). Frontend: 5 new tests total (unchanged this round — `workerTemplates.js`'s 3 new entries are additive), lint clean, 357/357, clean build. **Live-verified end to end** against real backend/frontend instances on non-conflicting ports through the actual rendered GUI, not just `TestClient` — round 3's own live run produced a genuine "failed" outcome the verification gate correctly caught; round 4's live run is what actually found its own ordering bug, then confirmed the fix; round 5's live run drove a real Project from creation to `"completed"` through all three newly-seeded roles (Web Developer → QA → Documentation) with **zero human clicking Execute** — the scheduler daemon alone triggered every step — after two earlier genuine verification failures against the same fixture correctly left the Project `"blocked"` rather than falsely `"completed"`. `AUTONOMOUS_ORGANISATION_V1.md`'s own §9 build sequence is now fully implemented.

**MULTI_ORGANISATION_FOUNDATION_V1 (2026-08-24)** — recorded the Organisation Model, four-phase Worker Evolution Model (Phase 1 One Worker Per Role — where this platform stands today — through Phase 4 Auto-Scaling Digital Teams), and the "Worker != AI Model" architectural rule as `teracom-ai-docs/TERACOM_DECISIONS.md` SD-015, not a new strategy document. A direct code audit (not a documentation review) confirmed API-layer organisation isolation is genuinely solid across Knowledge, Documents, Governance, and every ownership-check helper, and found the one real, previously-unaudited gap: `services/scheduler_service.py`'s unattended batch selection (added the same day as `AUTONOMOUS_PROJECTS_V1`) had no organisation-fairness boundary at all — one organisation's backlog could starve every other organisation's pending tasks indefinitely, since the whole platform shared one global `ORDER BY created_at LIMIT limit` query. Fixed with a fair, round-robin-across-organisations selector (organisations ordered by their own oldest eligible task, one task taken from each in turn every round) — with a single organisation, byte-for-byte the same behaviour as before; the fairness only changes results once more than one organisation has eligible work. Worker Pools and AI Provider Configuration are confirmed **not yet implemented in any form** (`Task.assignee_worker_id` is a single fixed FK; every worker in every organisation still runs against one hardcoded global `config.py#OLLAMA_URL`/`OLLAMA_MODEL`) — named as real, scoped gaps for a future workstream, not attempted this round. Backend: 1 new test (`test_select_pending_assigned_tasks_is_fair_across_organisations`), full suite 497/497.

**MULTI_ORGANISATION_PLATFORM_V1 (2026-08-24, same day)** — closes the two gaps the round above named: **Worker Pool Foundation** (`models/worker_pool.py#WorkerPool`, organisation-scoped with a `capacity` ceiling itself bounded by the organisation's own entitlement `worker_limit`; `Worker.worker_pool_id`/`Task.assignee_worker_pool_id`; `services/worker_pool_service.py#resolve_pool_assignment()` picks "an available worker within the pool" — the least-loaded active member — at *execution* time, wired into both the admin-triggered execute route and the scheduler's own pre-pass); **AI Provider Configuration Foundation** (`models/ai_provider_configuration.py`, one real row per organisation, routed through the single seam that matters, `services/execution_service.py`'s own worker-turn call — honest that only "ollama" has a working integration today, every other named provider raising a clear `ProviderNotImplementedError` rather than silently falling back); and **Organisation Provisioning** (`services/organisation_provisioning_service.py#provision_organisation()`, `POST /organisations/provision`, admin-only — bootstraps a starter "Engineering" department, four starter workers matching the same Dev→QA→Documentation chain already proven live, a governance default, and an organisation-memory record, reusing every existing creation path unmodified; deliberately *not* wired into `/signup` itself, kept as a separate, explicit action so the shared signup path and its many existing tests stay unaffected). `services/worker_service.py#create_worker()` was factored out of `api/workers.py`'s own inline logic so the admin-facing route and provisioning share one entitlement/trial/pool-capacity-checked path, not two copies. Recorded as `teracom-ai-docs/TERACOM_DECISIONS.md` SD-016, not a new strategy document. A real bug found live, not by unit tests: executing a task for an organisation configured with a real-but-unimplemented provider crashed with an unhandled 500 — fixed by catching `ProviderNotImplementedError` in `execute_task_route()` and returning a clean 400 (the scheduler's own broad exception handling already degraded gracefully). Backend: 15 new tests (`test_multi_organisation_platform.py`), full suite 512/512. **Live-verified** with two real organisations named after the workstream's own examples — AVC Mining (Developer Pool 5, QA Pool 2, left on Ollama) and XYZ Mechanical (Developer Pool 2, QA Pool 1) — confirming independent pools, independent AI provider configuration (one set to "gemini", the other unaffected), and a real pool-routed task resolved to a real worker and executed to a genuine, real-verified pass.

## Active work

In priority order:

1. **Start the *commercial billing* backend schema/endpoint work** (price, invoicing, a `Subscription` entity, Stripe or equivalent) — still the single longest-standing, most-blocking gap in the whole roadmap; nothing in Package 9's backend-requirements list can proceed without a real pricing/subscription concept first.
1a. ~~Real email-sending capability / password-reset delivery.~~ **Done, mechanism-wise — Package EMAIL1 / Wave 1 Workstreams 2–3.** A real `SmtpEmailProvider` exists and is wire-protocol-proven; the reset email itself now contains a real working link. **Still not actually delivering anything** — no real SMTP credential is configured in this environment (see known-gaps item below).
1b. ~~Trial expiry enforcement.~~ **Done — Package CX1.** Grace period → Locked Mode, real, live-verified.
2. ~~Retrofit worker-limit entitlement enforcement onto `WorkerCreationRequest` approval.~~ **Done — `1f37edd` (2026-08-20, Human Authority Model & Customer Readiness Review Implementation wave).** Paid-tier `worker_limit` is now enforced on both `POST /workers/` and `WorkerCreationRequest` approval, confirmed live against a real licence deliberately pushed to its limit. `entitlement.user_limit` on `POST /users/` was closed the same session (`1c56e68`). This closes a gap [[project-state]] §5 item 40 had also carried — corrected there too in this same pass.
3. **Replace `lib/licensing/referenceLicence.js` with real data, then wire `RenewalWizard.js`/`OwnershipTransferWizard.js`** (Package Q) — unchanged; both wizards still depend on illustrative tier/hosting/licence-id data.
4. **Resolve `LICENSING_MODEL_V1.md` §19's still-open items** — licence file format/signing, hardware-fingerprint algorithm, human-approval sign-off — unchanged.
5. **A small, standalone hardening pass on Package 7's admin pages** (`/portal/admin/{users,organisation,permissions}`) — the same Next.js layout/children data-exposure pattern Package 9 found and fixed for its own pages; not yet confirmed fixed or unfixed here. Not urgent — nothing has ever been visibly wrong.
6. **A real update/delete (or explicit archive) capability**, still overdue across roughly two dozen distinct create-and-read/decide-only rows spanning nearly every package since Package 6 (Memory) — see [[project-state]] §5 for the itemised list. A single backend conversation about correction/removal semantics could address all of these at once.
7. **A real external provider API integration for Federation** — every "federation response" is still generated locally via Ollama and marked `is_simulated=True`, reconfirmed unchanged as of `FEDERATION2_ASSESSMENT.md` (2026-08-23). A live, named credibility gap, not just a future nice-to-have — see `AI_CONTEXT_TRANSFER_V1.md` §1 item 4.
8. **Retrofit the (now 37, not 24) pre-existing `require_role("admin")` call sites onto the governance policy registry** — count corrected per the 2026-08-21 documentation audit (`TERACOM_CURRENT_STATE.md` §7); deliberately deferred (ADR-020), unchanged in scope decision.
9. **A real conversational AI Concierge** — the current widget remains a real, live, statically-authored FAQ/routing surface, not a live LLM.
10. **A real update/delete (or explicit archive) capability for `Lead`** — the same gap as item 6, one more row.
11. **A dedicated customer-portal visual pass** — `PortalDashboardWidget.js`/`PortalDealsView`/`PortalSupportRequestList` still run on pre-existing list/table styling.
12. **Promote Marketing's "Production" queue to its own page/nav entry, or add a pending-approval badge** — still no cross-campaign view of content/video awaiting admin approval.
13. **Deploy the prepared TLS/reverse-proxy config** — blocked on root/sudo access and a real public domain with DNS pointing here; the Website/Application Separation's own domain target (`app.teracomsolutions.com.au`) is the concrete candidate once Phase 5 is executed.
14. **Refresh-token rotation** — deliberate scope simplification (ADR-026), unchanged.
15. **A real secrets manager (vault/KMS)** — unchanged, and now independently reinforced as a "High effort" recommendation by `PLATFORM_SECURITY_REVIEW_V1.md` §5, which names it as a real blocker for both a Microsoft 365 credential store and real (non-simulated) external-AI-provider billing.
16. ~~A CI pipeline — no `.github/workflows` or equivalent exists in either repo.~~ **Partially resolved.** `teracom-ai-frontend` now has a real, substantive `.github/workflows/ci.yml` (lint, test, build, and a deployment-validation smoke test against a real running production build), added during Website/Application Separation Phase 2 (2026-08-21) and updated for the post-split repo in Phase 4. **`teracom-ai-backend` still has no CI pipeline at all** — every backend validation to date has been run manually.
17. **Real SMTP credentials** — a real mailbox/relay is the single remaining step to make every notification this backend fires actually reach an inbox; wire-protocol correctness is already proven (Wave 1 Workstream 2).
18. **A Microsoft Graph-based email provider** — deferred to Package FEDERATION2's own scope, unchanged.
19. **Retry/backoff for failed email sends** — unchanged.
20. **Per-task completion emails for Customer Success onboarding** — unchanged, judged out of scope as noisy.
21. **Reconcile "Governance" vs. "Policy" vs. "governance rules," and "AI worker" vs. "Worker," terminology** (`PRODUCT_EXPERIENCE_REVIEW_V1.md` §3/§4 items 4–5) — confirmed on real, live screens; low effort, not started.
22. **Close the automated-account-creation and general API-rate-limiting security gaps** (`PLATFORM_SECURITY_REVIEW_V1.md` §5) — no CAPTCHA/email-verification on `POST /signup`/`POST /signup/trial`; only 7 auth-adjacent endpoints are throttled anywhere in the backend. Low–medium effort, not started.
23. **Decide and execute the operational-tier imagery drop, and build the Login page's full entry-tier generative treatment** (`VISUAL_IDENTITY_V3_ROADMAP.md` §5, `LOGIN_EXPERIENCE_V1.md` §6) — direction decided (imagery survives only at the Login/entry tier, in the native red/black palette; every operational page reverts to glow-only), not yet executed beyond Login's own palette groundwork (`015233e`). Medium effort.
24. **Add a brief post-signup welcome/orientation screen** before a new user lands cold on the operational dashboard (`PRODUCT_EXPERIENCE_REVIEW_V1.md` §3/§4 item 8) — not started.
25. **Wire the `enforce_mfa` governance rule into real login enforcement** (`PLATFORM_SECURITY_REVIEW_V1.md` §5) — stored and readable, zero enforcing call sites found by a full-backend grep. Medium effort, not started.
26. **Resolve Reporting's long-term nav home** (`NAVIGATION_REVIEW_V1.md` §5) — an open design question, likely coupled to `TERACOM_DECISIONS.md` SD-004's own deferred Executive Intelligence work; not yet scoped.

## Known backend gaps carried from prior packages (not yet fixed, backend repo)

- **`POST /chat/` cannot be resumed and never returns its session's id** (Package 5) — still the highest-value backend fix identified for the existing product experience, unchanged.
- **No update or delete endpoint exists for most create-and-read/decide-only entities** (Memory tiers, Proposals/Quotes/Contracts, Campaigns/Content/Video/Media Centre items, Federation Consultations, Department Budgets, Projects/Tasks, Support Requests, Worker Creation Requests/Deployment Records/Platform Incidents, `OrganisationOnboardingTask`, `Lead`) — unchanged, see Active Work item 6.
- ~~No user-role-change, deactivation, or delete endpoint exists.~~ **Resolved, Human Authority Model (2026-08-20, `ebfad14`).** Real `GET`/`PATCH /users/{id}` role & status endpoints exist with escalation/self-action guards, wired into a real frontend (`02586b6`). No user-*delete* endpoint exists still — only role/status change, not row removal.
- ~~Worker-limit entitlement enforcement is real for Marketplace-pack provisioning only.~~ **Resolved, `1f37edd` (2026-08-20).** See Active Work item 2 above.
- **`CrmContact` has no department-scoping** (Package J) — unchanged.
- **`Campaign`/`ContentPiece`/`VideoAsset`/`MediaCentreItem` have no department-scoping** (Package K) — unchanged.
- **No real external AI provider is wired up for Federation** (Package L) — unchanged, reconfirmed 2026-08-23.
- **No `Subscription` entity, invoicing, or commercial billing exists anywhere in this backend** — unchanged, the single biggest remaining gap.
- **`PortalContact` is 1:1 with a `CrmContact`, not multi-user-per-customer** (Package O) — unchanged.
- **The governance policy registry does not retrofit the pre-existing `require_role("admin")` call sites** — now confirmed at **37** sites, not 24 (2026-08-21 audit, `TERACOM_CURRENT_STATE.md` §7); deliberate scope boundary (ADR-020), unchanged.
- **Platform health is a computed snapshot, not a real metrics/alerting/paging pipeline** (Package PQR) — unchanged.
- **No GPU metrics exist** (Platform Review Wave 1) — unchanged.
- **Password reset has a real, tested token mechanism and correct email content, but no real delivery** — real SMTP credentials remain the single missing piece (Active Work item 17).
- **No retry/backoff for a failed email send** (Package EMAIL1) — unchanged.
- **The AI Concierge is a static FAQ/routing widget, not a live LLM** (Package CX1) — unchanged.
- **`enforce_mfa` is stored and readable but never checked at login** (Settings & Security V1, confirmed by `PLATFORM_SECURITY_REVIEW_V1.md` §3) — new, see Active Work item 25.
- **No general API rate limiting exists** — only 7 auth-adjacent endpoints are throttled; every other authenticated endpoint has none (`PLATFORM_SECURITY_REVIEW_V1.md` §3/§4.6) — new, see Active Work item 22.
- **Secrets remain plaintext `.env` on disk** — no vault/KMS (`PLATFORM_SECURITY_REVIEW_V1.md` §3) — new, see Active Work item 15.
- **No CAPTCHA or email-verification gate on signup/trial-signup** — a rate-limit-respecting script can still create a new trial organisation roughly every 12 minutes indefinitely (`PLATFORM_SECURITY_REVIEW_V1.md` §4.4) — new, see Active Work item 22.
- **The in-process login rate limiter's state is not shared across worker processes** — correct for the current single-VM deployment, will silently stop working the moment the backend scales horizontally (`PLATFORM_SECURITY_REVIEW_V1.md` §3) — new.
- **`teracom-ai-backend` has no CI pipeline** — `teracom-ai-frontend` now does (see Active Work item 16); backend validation is still entirely manual — new.

## Parallel track reminder

The backend schema conversation for *commercial billing* specifically (price, invoicing, subscriptions, payment) remains the single blocking dependency for turning Package 9 from a UX scaffold into a real product feature — unaffected by every workstream since Package Q, all of which extended the product in unrelated directions (organisational intelligence, memory, sales/marketing/finance/operations platforms, federation, the Customer Portal, worker lifecycle governance, a real 5-tier human authority model, the Organisation Workflow Wizard, Settings & Security V1, visual identity, and the physical website/application split). Worker-limit and user-limit *entitlement enforcement* — the closest any prior package had come to the money question without touching it — is now real and closed across every creation path (Active Work item 2), leaving *only* the money half (price, invoicing, subscriptions) as the remaining gap, more narrowly than it has ever been stated before.
