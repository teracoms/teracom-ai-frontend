# Current Sprint

**As of:** 2026-08-18
**Replace, don't accumulate:** this file reflects only what's active *right now*. When a sprint ends, move its outcome into [[changelog]] and overwrite this file for the next one — do not let this turn into a history log (that's what the changelog is for).

---

## Where things stand

Packages 1 through 9, Package G through PQR, Package Q (Commercial Provisioning & Entitlements — see [[changelog]] for the individual names), and now **Platform Review Wave 1 (Teracom UX, Branding and Navigation Refresh)** have all been addressed. Wave 1 is a different kind of package from everything before it: not a new domain (like G–PQR) or new backend connective tissue (like Q), but a frontend-focused usability/presentation pass across everything already shipped — rebranding, navigation IA, a password-reset mechanism, a trial signup foundation, and a real admin-only system-resources panel. See `docs/backend/PLATFORM_REVIEW_WAVE_1_UX_BRANDING_AND_NAVIGATION_REFRESH_IMPLEMENTATION_REPORT.md`. Package Q closes the gap between the licensing/entitlement engine (Packages A–D) and the worker catalogue (Package D): a completed sale (an approved `initial_issuance`/`worker_pack`/`tier_change`/`hosting_change` licence request) now results in a real, entitlement-checked, fully provisioned customer environment — real `WorkerPackAddOn` quantity add-ons, real Marketplace-pack-to-`Worker` provisioning, and an auto-seeded organisation onboarding checklist — rather than three previously-disconnected pieces (a `worker_pack`/`tier_change`/`hosting_change` request that staff could "approve" with no effect, a Marketplace pack a customer had to manually recreate workers from one at a time, and no onboarding surface at all for a newly-activated organisation). Build/lint/test passing on both repositories, end-to-end smoke-tested against a live backend and a live frontend (no Ollama needed — nothing in this package touches it). See [[project-state]] §2/§5 and `docs/backend/PHASE_0_PACKAGE_Q_COMMERCIAL_PROVISIONING_AND_ENTITLEMENTS_IMPLEMENTATION_REPORT.md` for full detail.

**Package 9 (Billing & Licensing) is still different: most of it is a frontend UX scaffold against illustrative reference data, not a real *commercial billing* system** — the licence-issuance/entitlement/tier-gating mechanism underneath it is real and complete (backend Phase 0 Packages A–D, now further extended by Package Q's `worker_pack`/`tier_change`/`hosting_change` wiring), and `WorkerPackWizard.js`'s submission is now genuinely real (Package Q) — but `RenewalWizard.js`/`OwnershipTransferWizard.js` and the rest of the Billing & Licensing UI (Overview, Licence Details, Usage & Capacity) remain illustrative. Only the commercial billing layer itself (price, invoicing, subscriptions, payment) remains genuinely unbuilt — deliberately out of scope for Package Q, per direct instruction. See [[project-state]] §3's correction and ADR-017.

`FRONTEND_ARCHITECTURE_V1.md` Part E's original nine-package sequencing was already exhausted before G through Q; those packages extended the product beyond that original roadmap. Package Q is the first of these extension packages whose primary purpose is connecting two *already-shipped* subsystems (licensing/entitlements and the worker catalogue) rather than adding a new organisational-intelligence domain.

**Package CX1 (Customer Experience & Commercial Readiness Wave) has since shipped, committed, and pushed** — it closed three items this file had tracked since Wave 1/Package O (see items 1a/1b/10 below) with an honestly-scoped foundation: a real `EmailMessage`/`NotificationLog` pair (no real provider — see item 1a below), a computed trial-lifecycle state machine (grace period → Locked Mode → worker-limit block), `PortalContact` self-service password recovery, a real AI Concierge widget (foundation only, no live LLM), and marketing-site leads capture with staff-facing visibility. See [[changelog]] and `docs/backend/PACKAGE_CX1_CUSTOMER_EXPERIENCE_AND_COMMERCIAL_READINESS_IMPLEMENTATION_REPORT.md` (ADR-024) for full detail, including two real bugs found via full-suite testing (a forgotten notification hook, and a shared rate-limiter instance across identity planes) and a mid-wave production incident (stale frontend build) that was fully root-caused and fixed.

Platform Review Wave 3 (Digital Workspace Completion) has since shipped, committed, and pushed (frontend `9c67907`) — a frontend-only, no-new-dependency polish pass across the entire `/portal` workspace, built directly on `TERACOM_REVIEW_BACKLOG.md`'s own Wave-1 findings: two leftover "CTO" branding strings fixed, two real `PortalNav.js` bugs fixed (dropdowns not closing, missing WAI-ARIA menu-button pattern), the last three admin pages missing an inline role check now fixed, a never-actually-used `StatusDot` component now wired into real worker-status visualisation, the stale root `/portal` "Overview" page retired via redirect to `/portal/dashboard` (one canonical landing page), a new dashboard "Explore your workspace" quick-links section closing a major discoverability gap, five new icons closing the last icon-rollout gaps, and `CustomerPortalNav.js` gaining a "Teracom AI" brand link it never had. See [[changelog]] and `docs/backend/PACKAGE_WAVE3_DIGITAL_WORKSPACE_COMPLETION_IMPLEMENTATION_REPORT.md` (ADR-025).

Package SEC1 (Platform Security Hardening) has since shipped, committed, and pushed (backend `85eac8a`, frontend `3725f25`) — real session security across all three identity planes, CORS enforcement, `/docs` gated behind `ENVIRONMENT`, four API-surface role-gating fixes, `.env` hardening, and a prepared-not-deployed TLS/reverse-proxy config. See [[changelog]] and `docs/backend/PACKAGE_SEC1_PLATFORM_SECURITY_HARDENING_IMPLEMENTATION_REPORT.md` (ADR-026).

**Package EMAIL1 (Communications and Notification Framework) has been implemented, tested, and live-verified against the real running services — but is held uncommitted pending explicit approval.** A real, generic `SmtpEmailProvider` (stdlib-only, works against a real Microsoft 365 mailbox via SMTP AUTH today) now exists alongside the pre-existing log-only fallback — this environment has no real SMTP credentials configured, so it still logs rather than delivers, honestly. Real templates for all eleven notification types, real delivery/audit tracking on `EmailMessage` (sent/failed/logged, not always "logged"), a proactive trial-expiring-soon notice, Contact Sales/Demo Request lead notifications, and onboarding notices for both of this backend's two distinct onboarding concepts (`OrganisationOnboardingTask` and `OnboardingTask`/Customer Success, the latter emailing a `CrmContact` directly for the first time). Two new communication-timeline surfaces: an admin-only `/portal/admin/communications` page and a "Communications" section on the Sales contact detail page. See [[changelog]] and `docs/backend/PACKAGE_EMAIL1_COMMUNICATIONS_AND_NOTIFICATION_FRAMEWORK_IMPLEMENTATION_REPORT.md` (ADR-027) for full detail.

**Wave 1 — Workstream 5 (Licensing and Entitlement Architecture) has shipped — Wave 1 is now complete.** A real `Plan` entity, seeded to mirror `services/entitlement_service.py`'s hardcoded `_TIER_LIMITS` exactly; `Licence.plan_id` (additive, populated for new licences and backfilled for existing ones — the `tier` string column and every one of its 13 confirmed enforcement call sites are deliberately left unchanged in this pass, a scoped expand-only migration, not a full contract); a new `LicenceBillingReference` side table prepared for a future billing integration; a new `GET /licensing/plans` endpoint. No new audit table — the existing `LicensingAuditLog` already covers this. Backend: 277/277 tests passing (6 new). Frontend: no changes needed, 302/302 passing. See `WAVE1_LICENSING_ENTITLEMENT_ARCHITECTURE_IMPLEMENTATION_REPORT.md`, ADR-032, and `WAVE1_COMPLETION_REPORT.md` for the full wave retrospective.

**Wave 1 — Workstream 4 (META1 - Metadata Foundation) has shipped.** The first new capability in this wave, not a bug fix: `Knowledge` gained `document_type`/`sensitivity_level` columns and a real `knowledge_tags` join table (indexed on `tag`, not a JSONB array), a new `services/metadata_service.py` as the single validated read/write point regardless of caller, and new `GET`/`PATCH /knowledge/{id}/metadata` endpoints plus optional metadata fields on `POST /knowledge/`. Built ahead of the Organisation Knowledge Graph, Document Lifecycle Governance, and Organisational Search — none of which exist yet — per `teracom-ai-docs/KNOW1_ASSESSMENT.md` §6's own finding that this foundation is cheaper to build first than to retrofit later. Backend: 271/271 tests passing (11 new). Frontend: no changes needed — classification remains admin/API-only until a real consumer exists; 302/302 passing. See `WAVE1_META1_METADATA_FOUNDATION_IMPLEMENTATION_REPORT.md` and ADR-031.

**Wave 1 — Workstream 3 (Password Reset Completion) has shipped.** A real, distinct defect fixed, independent of Workstream 2's SMTP-credential question: `request_password_reset()`/`request_portal_contact_password_reset()` generated a real reset token and then discarded it — never threading it into the outbound email, so the email always instructed the recipient to "use the link your administrator provides" with no link ever present, on both identity planes, regardless of whether SMTP was configured. Both service functions now build a real reset URL (a new `FRONTEND_BASE_URL` backend config setting — none existed before) and pass it through the shared `notify_password_reset_requested()` into a rewritten `_password_reset_requested` template that renders an actual working link. The frontend's `/portal/reset-password` and `/customer-portal/reset-password` pages already correctly consumed a `?token=` parameter — confirmed by direct inspection, no frontend change needed. Backend: 260/260 tests passing (2 new). Frontend: unaffected, 302/302 passing. See `WAVE1_PASSWORD_RESET_COMPLETION_IMPLEMENTATION_REPORT.md` and ADR-030.

**Wave 1 — Workstream 2 (SMTP Completion) has shipped.** Confirmed `SmtpEmailProvider` needed no code change — it already branches correctly on `SMTP_HOST` and works against any SMTP-capable mailbox or relay. What this workstream added: a real TCP/SMTP wire-level test (`tests/test_email_provider.py`) proving the actual protocol handshake completes correctly end to end, something no existing test did (they all mocked `smtplib.SMTP` itself), and a full activation runbook (`docs/operations/PRODUCTION_RUNBOOK.md` §9) with the exact `.env` values and post-activation verification steps needed to go live. **Real delivery remains not yet configured** — provisioning a real external mailbox or transactional-email account requires access this session doesn't have; this is now a pure configuration step for whoever holds that credential, not an engineering one. Backend: 258/258 tests passing (1 new). Frontend: unaffected, 302/302 passing. See `WAVE1_SMTP_COMPLETION_IMPLEMENTATION_REPORT.md` and ADR-029.

**Wave 1 — Workstream 1 (Internal Pilot Blockers) has shipped.** Two real, verified defects fixed: `DELETE /documents/{id}`'s unhandled `IntegrityError` (a new `Knowledge.permissions` cascade relationship, plus a new `chroma_service.delete_embedding()` closing the corresponding vector-store leak), and a previously-unrecognised cross-plane login rate-limiter bug — `login_rate_limiter` was shared, unmodified, across all three identity planes' `/login` endpoints (`api/auth.py`, `api/staff_auth.py`, `api/portal_contact_auth.py`), the same class of bug ADR-024 already found and fixed for the *password-reset* limiters but never checked for on *login* specifically. Two new dedicated instances (`staff_login_rate_limiter`, `portal_contact_login_rate_limiter`) now exist, mirroring ADR-024's own template exactly. Backend: 257/257 tests passing (4 new). Frontend: no changes required — both defects are backend-only and invisible to any frontend code, confirmed by inspection; 302/302 tests passing (unchanged). See `WAVE1_INTERNAL_PILOT_BLOCKERS_IMPLEMENTATION_REPORT.md`.

## Active work

**Awaiting approval to commit Package EMAIL1** (backend + frontend, currently uncommitted working-tree changes). Once approved, the concrete next steps, in priority order:

1. **Start the *commercial billing* backend schema/endpoint work** (price, invoicing, a `Subscription` entity, Stripe or equivalent) — unaffected by Package Q, which was explicitly scoped to exclude this. Nothing else in Package 9's backend-requirements list can proceed without a real pricing/subscription concept first.
1a. ~~Real email-sending capability blocks `PortalContact` self-service password recovery and the password-reset workflow's own delivery step.~~ **Done, mechanism-wise, and the email content itself is now correct too — Package EMAIL1 / Wave 1 Workstreams 2-3.** A real, generic `SmtpEmailProvider` exists (`services/email_provider.py`) and is wired into every notification this backend fires, with its wire-level correctness now proven (Workstream 2). Separately — a real, distinct defect fixed by Workstream 3, not just the missing credential — the password-reset email itself previously contained no working link at all, even before any SMTP question: the raw reset token was generated and immediately discarded, never reaching the outbound email. Both `request_password_reset()` and `request_portal_contact_password_reset()` now build a real `.../reset-password?token=...` URL and thread it into the email, which the frontend's already-existing reset-password pages correctly consume. **Still not actually delivering anything**, because no real SMTP credentials exist in this environment — set `SMTP_HOST`/`SMTP_USERNAME`/`SMTP_PASSWORD` (a real mailbox or relay, Microsoft 365's own `smtp.office365.com:587` included) to make it real. That credential is now the single remaining step, not any further engineering.
1b. ~~Trial expiry enforcement — nothing currently blocks or downgrades access once `Organisation.trial_ends_at` passes.~~ **Done — Package CX1.** `services/trial_service.py` computes a real `"active"`/`"grace"`/`"locked"` state (7-day grace period, then Locked Mode) on every relevant request, blocks new worker creation once locked, and caps trial organisations at `TRIAL_WORKER_LIMIT=3` workers even during the active/grace period — live-verified end to end (see the implementation report §7). No background scheduler exists to advance this on a timer; the state is computed fresh, not cached, the same pattern `licence_validation_service.py` already uses.
2. **Retrofit worker-limit entitlement enforcement onto `WorkerCreationRequest` approval** (Package Q, narrowed by Package CX1) — Package CX1 closed this for `POST /workers/` on trial organisations specifically; the pre-existing `WorkerCreationRequest` approval path, and worker-limit enforcement for non-trial (licensed) organisations on `POST /workers/` directly, remain open. — Package Q made `LICENSING_MODEL_V1.md` §4's "hard stop at the worker limit" policy real for the new Marketplace-pack-provisioning path only, deliberately not the two pre-existing worker-creation paths (a behaviour change to already-shipped, heavily-tested endpoints, judged out of scope for that package). Worth a dedicated, low-risk pass now that the enforcement pattern (`services/worker_pack_provisioning_service.py`) has proven itself.
3. **Replace `lib/licensing/referenceLicence.js` with real data, then wire `RenewalWizard.js`/`OwnershipTransferWizard.js`** (Package Q) — both wizards still depend on illustrative tier/hosting/licence-id data for fields a real submission would need; `WorkerPackWizard.js` was the one wizard whose payload didn't need any of that, which is why it alone was wired this package.
4. **Resolve `LICENSING_MODEL_V1.md` §19's still-open items** that block specific Package 9 screens from ever becoming real: licence file format/signing (a real signed artefact already exists per `Licence.signed_payload_ref` — confirm it satisfies this item or identify the remaining gap), hardware-fingerprint algorithm (blocks real Sovereign/Customer-Hosted enforcement), and who performs human approval (the Teracom-staff-decide flow already exists and is exercised by every package's own e2e test — confirm whether this fully resolves the open question or only partially).
5. **A small, standalone hardening pass on Package 7's admin pages**, applying the same fix Package 9 (and now Package H) applied to their own new pages: `/portal/admin/users`, `/portal/admin/organisation`, and `/portal/admin/permissions` likely still execute their real backend calls for a non-admin request even though the visible page has always correctly shown only the restricted message — the same Next.js App Router layout/children rendering gap Package 9 found and fixed for its own pages (§9 of that report). Not urgent (nothing has ever been visibly wrong), but worth closing properly.
6. **A real update/delete (or explicit archive) capability**, now overdue across twenty-two distinct rows instead of one: `WorkerMemory` (Package 6), `DepartmentMemory`/`OrganisationMemory`/`MemorySummary` (Package H), `Proposal`/`Quote`/`Contract`/`OnboardingTask` (Package J), `Campaign`/`ContentPiece`/`VideoAsset`/`MediaCentreItem` (Package K), `FederationConsultation` (Package L), `DepartmentBudget` (Package M), `Project`/`Task` (Package N), `SupportRequest`/`SupportRequestMessage` (Package O), `WorkerCreationRequest`/`DeploymentRecord`/`PlatformIncident` (Package PQR), and `OrganisationOnboardingTask` (Package Q, complete-only, no delete/archive) all share the identical "create and read/decide only" gap. A single backend conversation about correction/removal semantics could address all of these at once rather than one at a time.
7. ~~Use department assignment as a real CTO delegation signal.~~ **Done — Package I.** `_pick_worker_for_subtask()` now routes to a department's head when the department's own name/description best matches a subtask.
8. ~~Wire `Department.function` into CTO Orchestration's delegation routing.~~ **Done — Package PQR.** `_pick_worker_for_subtask()` now also consults `department.function` via a static keyword-boost dictionary, closing the gap flagged since Package J (reflagged K/M/N).
9. **A real external provider API integration for Federation** (Package L) — every "federation response" today is generated locally via Ollama and marked simulated; wiring even one real external provider (OpenAI/Anthropic/Azure) would be the natural next step once there's a concrete business case and API budget for it.
10. ~~A self-service password reset/recovery mechanism for `PortalContact` accounts.~~ **Done — Package CX1.** `POST /portal-contact/{forgot-password,reset-password}` and their frontend `/customer-portal/{forgot-password,reset-password}` pages mirror the internal-`User` flow exactly, on a dedicated `PortalContactPasswordResetToken` model and a dedicated rate limiter — live-verified end to end (create account → forgot-password → obtain token → reset → login with new password succeeds, old password rejected). Delivery of the raw token to the account holder is still not real — see item 1a.
11. **Retrofit the 24 pre-existing `require_role("admin")` call sites onto the new governance policy registry** (Package PQR) — deliberately deferred (ADR-020) to avoid a mechanical, high-file-count refactor of already-shipped code for zero behavioural change; worth revisiting as a dedicated, low-risk cleanup pass once the registry has proven itself on this package's own new endpoints.
12. **A real, persistent `frontend/.env` file did not exist until Package CX1 created one** (only `.env.example` did) — production frontend↔backend connectivity had only ever worked by accident of whatever `.env.local` a prior session happened to leave behind. A real `.env` now exists with the correct `BACKEND_API_URL`; worth a dedicated look at whether Package OPS1's runbook/systemd unit should assert this file's presence (or required variables) on startup rather than failing silently with a 502 the way this gap did mid-wave.
13. ~~Wire a real email provider.~~ **Done — Package EMAIL1** (see item 1a's update above — the mechanism is real; only a real SMTP credential is still missing).
14. **A real conversational AI Concierge** (Package CX1) — the current widget is a real, live, statically-authored FAQ/routing surface (Start Trial/Request Demo/Talk to Sales/FAQ), not a live LLM; a genuine chat-based concierge is a distinct, larger piece of work once there's a concrete design for what it should be able to do.
15. **A real update/delete (or explicit archive) capability for `Lead`** (Package CX1) — the identical "create and read/decide only" gap item 6 already tracks for twenty-two other rows; `mark_lead_contacted()` is the only mutation `Lead` supports.
16. **A dedicated customer-portal visual pass** (Platform Review Wave 3, WBL-017) — `PortalDashboardWidget.js`/`PortalDealsView`/`PortalSupportRequestList` still run on pre-existing list/table styling, not the icon/stat-tile visual language every `/portal/**` page now uses. Deliberately scoped out of Wave 3 as a bigger, dedicated project on a second identity plane's entire surface.
17. **Promote Marketing's "Production" queue to its own page/nav entry, or add a pending-approval badge** (Wave 1, WBL-006, reflagged Wave 3) — still no cross-campaign view of content/video awaiting admin approval; an admin has to open every campaign individually to find anything pending.
18. **Deploy the prepared TLS/reverse-proxy config** (Package SEC1) — real, ready-to-use nginx config and a certbot runbook exist (`docs/operations/REVERSE_PROXY_AND_TLS.md`), blocked on two infrastructure dependencies only: root/sudo access on this host, and a real public domain with DNS pointing here. Set `ENVIRONMENT=production` in the same maintenance window this finally goes live in.
19. **Refresh-token rotation** (Package SEC1) — the current design reuses the same refresh token across multiple `/auth/refresh` calls rather than rotating on each use, a deliberate scope simplification (see ADR-026) to avoid the concurrent-use race a rotated design introduces. Worth a dedicated pass once there's a concrete need for the extra replay-resistance.
20. **A real secrets manager** (Package SEC1) — `.env` file permissions were hardened (`600`) and a rotation procedure now exists for `JWT_SECRET_KEY`, but every credential still lives in a plaintext file on disk. Fine for a single-operator host; not for a shared or public-facing one.
21. **A CI pipeline** (Package SEC1's own assessment turn, restated) — no `.github/workflows` or equivalent exists in either repo; every validation in every package to date, including this one, has been run manually.
22. **Real SMTP credentials** (Package EMAIL1; wire-protocol correctness proven, Wave 1 Workstream 2) — a real mailbox/relay (Microsoft 365's own `smtp.office365.com:587` included) is the single remaining step to make every notification this backend fires actually reach an inbox. `SmtpEmailProvider` is no longer just tested via a mocked `smtplib.SMTP` — `tests/test_email_provider.py` now includes a real TCP/SMTP wire-level handshake against a live local socket, proving the actual protocol exchange (EHLO/MAIL FROM/RCPT TO/DATA/QUIT) completes correctly, not just that the right method calls happen. A full activation runbook now exists (`docs/operations/PRODUCTION_RUNBOOK.md` §9). Only a real external mailbox/relay credential — something this session cannot provision on its own — remains missing.
23. **A Microsoft Graph-based email provider** (Package EMAIL1, deferred to Package FEDERATION2's own scope) — `SmtpEmailProvider` already covers "send via a real Microsoft 365 mailbox" today; a Graph `/sendMail`-based provider would only be needed for Graph-specific features (shared-mailbox send-as, delivery receipts), gated on the same Azure AD app registration Package FEDERATION2's own assessment already scoped.
24. **Retry/backoff for failed email sends** (Package EMAIL1) — a `SmtpEmailProvider` failure is recorded (`EmailMessage.status="failed"`, `error_message`) but never automatically retried; no background scheduler exists in this backend to drive a retry queue (a standing gap, not new).
25. **Per-task completion emails for Customer Success onboarding** (Package EMAIL1) — only the "onboarding started" notice was built for `OnboardingTask`; the organisation-level `OrganisationOnboardingTask` "all done" milestone was built, but four separate per-task completion emails per contact was judged out of scope as noisy.

## Known backend gaps carried from prior packages (not yet fixed, backend repo)

- ~~`DELETE /documents/{id}` throws an unhandled 500 for any document still assigned to a worker~~ (Package 4). **Fixed, Wave 1 — Internal Pilot Blockers (2026-08-19).** See `WAVE1_INTERNAL_PILOT_BLOCKERS_IMPLEMENTATION_REPORT.md`.
- **`POST /chat/` cannot be resumed and never returns its session's id** (Package 5). See `CHAT_IMPLEMENTATION_REPORT.md` §2. Still the highest-value backend fix identified for the existing product experience.
- **No update or delete endpoint exists for any memory tier** (Package 6, extended by Package H) — worker, department, and organisation memory, plus memory summaries, are all create/read only. See `MEMORY_IMPLEMENTATION_REPORT.md` §2/§10 and `PHASE_0_PACKAGE_H_MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md` §11.
- **No update or delete endpoint exists for any proposal, quote, contract, or onboarding task** (Package J) — the identical "create and read/decide only" gap, now on the sales/customer-success data model. See `PHASE_0_PACKAGE_J_SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md` §10.
- **`CrmContact` has no department-scoping** (Package J) — pipeline/customer-health data is organisation-wide, not per-department. See `SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md` §11.
- **No update or delete endpoint exists for any campaign, content piece, video asset, or Media Centre item** (Package K) — the identical "create and read/decide only" gap, now on the marketing/media data model. See `PHASE_0_PACKAGE_K_MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md` §10.
- **`Campaign`/`ContentPiece`/`VideoAsset`/`MediaCentreItem` have no department-scoping** (Package K) — organisation-wide, same trade-off `CrmContact` already made. See `MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md` §11.
- **No update or delete endpoint exists for any federation consultation** (Package L) — the identical "create and read only" gap, now on the federation data model. See `PHASE_0_PACKAGE_L_FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md` §10.
- **No real external AI provider is wired up** (Package L) — every "federation response" is generated locally via Ollama and marked `is_simulated`; the connector-stub classes are permanently dead code by design, same as Packages 8/J/K's own connectors. See `PHASE_0_PACKAGE_L_FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md` §2/§9.
- **No update or delete endpoint exists for any department budget** (Package M) — the identical "create and read/decide only" gap, now on the finance data model. See `PHASE_0_PACKAGE_M_CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md` §10.
- **No `Subscription` entity, invoicing, or commercial billing exists anywhere in this backend** (confirmed still true, Package M, corrected framing) — real `Licence`/`Entitlement` data exists and is now surfaced (`services/finance_summary_service.py`), but subscription-cost tracking genuinely has no backend data to draw on.
- **No update or delete endpoint exists for any project or task beyond status transitions** (Package N) — the identical "create and read/decide only" gap, now on the operations/project-delivery data model. See `PHASE_0_PACKAGE_N_OPERATIONS_AND_PROJECT_DELIVERY_IMPLEMENTATION_REPORT.md` §10.
- **`Project`/`Task` have no department-scoping requirement** (Package N) — `Project.department_id` is nullable, so a project may be organisation-wide or department-owned; unlike `CrmContact`/`Campaign` this is a deliberate design choice, not an oversight, but worth noting since it's the first package to make department-scoping optional rather than absent.
- **No update or delete endpoint exists for any support request or message beyond status transitions and appending new messages** (Package O) — the identical "create and read/decide only" gap, now on the Customer Portal's own data model. See `PHASE_0_PACKAGE_O_CUSTOMER_PORTAL_AND_SELF_SERVICE_IMPLEMENTATION_REPORT.md` §7.
- ~~No self-service signup, password reset, or email-based invite exists for `PortalContact` accounts~~ (Package O). **Password reset done — Package CX1** (`POST /portal-contact/{forgot-password,reset-password}`, a dedicated `PortalContactPasswordResetToken` model and rate limiter, live-verified full round trip). Self-service *signup*/invite for a `PortalContact` still doesn't exist — an admin still creates the account directly; only recovery of an existing account's password was closed.
- **`PortalContact` is 1:1 with a `CrmContact`, not a multi-user-per-customer model** (Package O) — exactly one login per contact; a customer organisation with multiple staff needing portal access is not supported without sharing one login.
- **No update or delete endpoint exists for any worker creation request, deployment record, or platform incident beyond status/decide transitions** (Package PQR) — the identical "create and read/decide only" gap, now on the worker-governance and production-operations data models. See `PHASE_0_PACKAGE_PQR_WORKER_GOVERNANCE_ARCHITECTURE_AND_PRODUCTION_OPERATIONS_IMPLEMENTATION_REPORT.md` §7.
- **The governance policy registry does not retrofit the 24 pre-existing `require_role("admin")` call sites** (Package PQR) — a deliberate, stated scope boundary (ADR-020); see item 9 above.
- **Platform health is a computed snapshot, not a real metrics/alerting/paging pipeline** (Package PQR) — `services/production_platform_health_service.py` derives `status` from real incident/deployment rows; no time-series storage or notification system exists anywhere in this backend.
- **No worker-update or worker-delete endpoint exists** (Package 3) — `EditWorkerForm.js` is deliberately non-persisting. See `WORKERS_IMPLEMENTATION_REPORT.md` §8.
- **`POST /permissions/` has no deduplication check and can leave a document silently un-revoked after one "Remove" click** (Package 7). See `ADMIN_IMPLEMENTATION_REPORT.md` §2/§10.
- **No user-role-change, deactivation, or delete endpoint exists** (Package 7). See `ADMIN_IMPLEMENTATION_REPORT.md` §10.
- **Connectors are entirely non-functional and not organisation-scoped even in principle** (Package 8). See `CONNECTORS_IMPLEMENTATION_REPORT.md` §2/§9.
- **Zero *commercial billing* backend support exists** (Package 9, corrected scope per Package M) — no price/invoicing/subscription data anywhere. The *licensing* half of this claim is no longer accurate: `Licence`/`LicenceRequest`/`Entitlement` are real, complete backend infrastructure (Packages A–D) — see [[project-state]] §3's correction. See `BILLING_AND_LICENSING_IMPLEMENTATION_REPORT.md` §3 for the itemised, prioritised commercial-billing backend requirements list.
- **No user↔department or user↔worker membership model exists** (Package H) — department memory's read gate is "any member of the owning organisation," not a finer-grained per-user assignment. See `PHASE_0_PACKAGE_H_MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md` §1.
- **The CTO department-routing heuristic is a plain keyword match on a department's own name/description** (Package I) — a vaguely-described department may not route correctly to its head. Deterministic and testable by design, not a model. See `PHASE_0_PACKAGE_I_DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md` §5.
- **No update or delete endpoint exists for `OrganisationOnboardingTask` beyond marking one complete** (Package Q) — the identical "create and read/complete only" gap, now on the commercial-provisioning data model.
- **Worker-limit entitlement enforcement is real for Marketplace-pack provisioning only** (Package Q) — `services/worker_pack_provisioning_service.py` blocks a provisioning action that would exceed `entitlement.worker_limit`; the pre-existing `POST /workers/` (direct admin creation) and `WorkerCreationRequest` approval paths remain unenforced, a deliberate scope boundary (see Active Work item 2 above), not an oversight.
- **`WorkerPackAddOn` purchases have no cap on how many an organisation can accumulate** (Package Q) — `LICENSING_MODEL_V1.md` §7 itself lists "whether there is a maximum number of packs stackable on one licence" as not decided; this package implements the formula as specified, with no upper bound, consistent with that open question remaining open.
- **Password reset (both identity planes) has a real, tested token mechanism but no email delivery** (Platform Review Wave 1, extended to `PortalContact` by Package CX1) — the token genuinely validates/expires/single-uses; a real, persisted `EmailMessage`/`NotificationLog` record now exists per attempt (Package CX1), but nothing actually sends it to the account holder's inbox. See item 1a above.
- ~~Nothing enforces `Organisation.trial_ends_at`.~~ **Done — Package CX1.** A real grace period (7 days) then Locked Mode is computed on every relevant request and blocks new worker creation; trial organisations are also capped at `TRIAL_WORKER_LIMIT=3` workers even before expiry. See item 1b above and the implementation report §7 for live verification.
- **No GPU metrics** (Platform Review Wave 1) — `services/system_resource_metrics_service.py#get_gpu_summary()` always returns `{"available": false, "status": "not_configured"}`; the objective itself named this "Future GPU support", not a current requirement.
- ~~No real email provider is connected.~~ **Mechanism done, credential still missing — Package EMAIL1.** A real, generic `SmtpEmailProvider` exists (`services/email_provider.py`); this environment simply has no real SMTP credentials configured, so it still falls back to logging. See item 1a/22 above.
- **No retry/backoff for a failed email send** (Package EMAIL1) — `EmailMessage.status="failed"` is recorded honestly but never automatically retried. See item 24 above.
- **`Lead` has no update or delete endpoint beyond `mark_lead_contacted()`** (Package CX1) — the identical "create and read/decide only" gap tracked for twenty-two other rows, now on the marketing-leads data model.
- **The AI Concierge is a static FAQ/routing widget, not a live LLM** (Package CX1) — real and interactive (Start Trial/Request Demo/Talk to Sales/FAQ), but not conversational. See item 14 above.
- **No real, persistent `frontend/.env` existed until Package CX1 created one** — production connectivity had only worked by accident of leftover `.env.local` files from prior sessions; worth confirming Package OPS1's runbook/systemd unit asserts this file's presence going forward. See item 12 above.

## Parallel track reminder

The backend schema conversation for *commercial billing* specifically (price, invoicing, subscriptions, payment) remains the single blocking dependency for turning Package 9 from a UX scaffold into a real product feature — unaffected by Package G through Q, which extended the product in unrelated directions (organisational intelligence, memory, the Department Head layer, native sales/marketing/finance/operations platforms, a federation registry, a Customer Portal, worker lifecycle governance and production operations, and now — Package Q — the connective tissue between the licensing/entitlement engine and the worker catalogue). Package Q is the closest any package has come to this item without actually touching it: it makes the *entitlement* half of the commercial story real and enforced (at least for one provisioning path), leaving only the *money* half (price, invoicing, subscriptions) as the remaining gap.
