# Project State

**Last verified:** 2026-08-17
**Verified against:** live repository state (`git log`, `find`, `package.json`) in `teracom-ai-frontend`, cross-checked against `FRONTEND_ARCHITECTURE_V1.md` and the implementation reports. Package 3 row re-verified 2026-08-15 (later same day) after shipping — routes/nav/proxy wired, full gate re-run, end-to-end smoke test against a live backend. Packages 4, 5, 6, 7, 8, and 9 verified the same way, and additionally against the actual `teracom-ai-backend` source directly (available on disk in this environment as a sibling directory, though not part of this git repository) — see §3's note. Packages 5 and 6's smoke tests also exercised a real, non-mocked local Ollama completion (Package 6's specifically to trigger a real auto-memory capture). Package 7's smoke test reproduced a real backend bug and confirmed it directly against Postgres, not just from source reading. Package 8's smoke test confirmed connector responses are identical across accounts/organisations, directly, not just from source reading. **Package 9 is categorically different from Packages 1–8 — see §2's row and §3's note below.** Package G (Autonomous CTO & Organisational Intelligence) and Package H (Knowledge & Memory Intelligence) both ship real backend+frontend features, verified the same way as Packages 1–8; this file was not updated when Package G shipped (2026-08-16) and is corrected here alongside Package H's own verification — see [[memory-intelligence-implementation-report]] and `PHASE_0_PACKAGE_G_AUTONOMOUS_CTO_IMPLEMENTATION_REPORT.md`/`PHASE_0_PACKAGE_H_MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md` for both. Package I (Department Head Layer & Executive Organisation) shipped the same day as this update and is included below — see `PHASE_0_PACKAGE_I_DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md`/`DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md`. Package J (Sales & Customer Success Platform) shipped the same day and is included below too — see `PHASE_0_PACKAGE_J_SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md`/`SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md`. Package K (Marketing & Media Platform) shipped the same day too — see `PHASE_0_PACKAGE_K_MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md`/`MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md`. Package L (Federation Registry & External AI Consulting) shipped the same day too — see `PHASE_0_PACKAGE_L_FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md`/`FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md`. Package M (CFO & Finance Platform) shipped the same day too, and corrected a stale claim in this file discovered during its own research — see §3's note below and `PHASE_0_PACKAGE_M_CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md`/`CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md`. Package N (Operations & Project Delivery Platform) shipped the same day too — see `PHASE_0_PACKAGE_N_OPERATIONS_AND_PROJECT_DELIVERY_IMPLEMENTATION_REPORT.md`/`OPERATIONS_AND_PROJECT_DELIVERY_IMPLEMENTATION_REPORT.md`. Package O (Customer Portal & Self-Service Platform) shipped the same day too, and originated this backend's first identity plane scoped narrower than an organisation — see `PHASE_0_PACKAGE_O_CUSTOMER_PORTAL_AND_SELF_SERVICE_IMPLEMENTATION_REPORT.md`/`CUSTOMER_PORTAL_AND_SELF_SERVICE_IMPLEMENTATION_REPORT.md` and ADR-019. Package PQR (Worker Lifecycle & Governance + Architecture Integration + Production Operations) shipped the same day too, closing the three-package-old `Department.function`→CTO-routing gap and originating this backend's first Production Operations platform — see `PHASE_0_PACKAGE_PQR_WORKER_GOVERNANCE_ARCHITECTURE_AND_PRODUCTION_OPERATIONS_IMPLEMENTATION_REPORT.md`/`WORKER_GOVERNANCE_ARCHITECTURE_AND_PRODUCTION_OPERATIONS_IMPLEMENTATION_REPORT.md` and ADR-020. Package CX1 (Customer Experience & Commercial Readiness Wave) shipped 2026-08-18, committed and pushed to both repositories — see its own row below, `PACKAGE_CX1_CUSTOMER_EXPERIENCE_AND_COMMERCIAL_READINESS_IMPLEMENTATION_REPORT.md`, and ADR-024. Platform Review Wave 3 (Digital Workspace Completion) also shipped 2026-08-18, committed and pushed (frontend `9c67907`) — see its own row below, `PACKAGE_WAVE3_DIGITAL_WORKSPACE_COMPLETION_IMPLEMENTATION_REPORT.md`, and ADR-025. Package SEC1 (Platform Security Hardening) also shipped 2026-08-18, committed and pushed (backend `85eac8a`, frontend `3725f25`) — see its own row below, `PACKAGE_SEC1_PLATFORM_SECURITY_HARDENING_IMPLEMENTATION_REPORT.md`, and ADR-026. **Package EMAIL1 (Communications and Notification Framework, 2026-08-18) is implemented, tested, and live-verified against the real running services, but is deliberately excluded from the "✅ Complete" rows above — it is held uncommitted pending explicit approval, per direct instruction** — see its own row below, `PACKAGE_EMAIL1_COMMUNICATIONS_AND_NOTIFICATION_FRAMEWORK_IMPLEMENTATION_REPORT.md`, and ADR-027.

This document is a snapshot, not a narrative. If it disagrees with the code, the code wins — update this file, don't trust it blindly. See [[documentation-standards]] for the rule on keeping snapshot docs current.

---

## 1. What Teracom AI is

Teracom AI is the product build-out of `teracom-ai-frontend` (public marketing/commerce site, package name `teracom-commerce-platform-v3`) into an authenticated multi-tenant platform backed by a separate repository, `teracom-ai-backend` (FastAPI + PostgreSQL + Chroma + Ollama). The platform's core object is a **Worker** — an AI agent persona (name/role/purpose/instructions) scoped to an organisation, with its own knowledge assignments, chat sessions, and memory. See [[worker-catalogue]] for the currently approved roster of worker types and [[frontend-architecture]] for the full technical design.

Two repositories exist:

- **`teracom-ai-frontend`** (this repo) — Next.js 14 App Router, plain JavaScript, one global stylesheet, Stripe checkout, Zoho invoicing, and (newly) an authenticated `/portal` product surface.
- **`teracom-ai-backend`** (separate repo, not present here) — FastAPI, SQLAlchemy/PostgreSQL, JWT auth, Chroma vector store, Ollama for chat. Reviewed in detail by `FRONTEND_ARCHITECTURE_V1.md` Part B; no source files from that repo are checked into this one, so backend documentation in this knowledge base is necessarily second-hand (see [[backend-status]]).

## 2. Frontend state — what's actually built

Confirmed against the filesystem on 2026-08-15:

| Package | Status | Evidence |
|---|---|---|
| **Package 1 — Authentication** | ✅ Complete, validated | `lib/api/{auth,client,jwt,validation,constants}.js`, `middleware.js`, `app/api/auth/{login,logout,session}/route.js`, `app/portal/(public)/login/page.js`, `app/portal/(protected)/layout.js`, `components/portal/{AuthProvider,LoginForm,AccountSummary}.js` all present. 19 unit tests passing. |
| **Package 2 — Dashboard** | ✅ Complete, validated | `lib/api/{dashboard,results}.js`, `app/portal/(protected)/dashboard/{page,loading,error}.js`, `components/portal/{StatTile,EmptyState,ActivitySection,OrganisationSummaryCard,PortalNav}.js` all present. 31 unit tests passing (12 new). |
| **Package 3 — Workers** | ✅ Complete, validated | `lib/api/workers.js` (9 functions), `app/portal/(protected)/workers/{page,loading,error}.js`, `app/portal/(protected)/workers/new/page.js`, `app/portal/(protected)/workers/[workerId]/{page,loading,error}.js`, `app/api/portal/workers/route.js`, `app/api/portal/workers/[workerId]/knowledge/route.js`, `components/portal/{WorkerCard,WorkerListView,CreateWorkerForm,WorkerKnowledgeAssignment,EditWorkerForm}.js`, `PortalNav.js` (Workers link, nested-route active-matching) all present. 45 unit tests passing. End-to-end smoke-tested against a live backend as both admin and non-admin. |
| **Package 4 — Knowledge** | ✅ Complete, validated | `lib/api/knowledge.js` (10 functions), `app/portal/(protected)/knowledge/{page,loading,error}.js`, `app/portal/(protected)/knowledge/upload/{page,loading,error}.js`, `app/portal/(protected)/knowledge/[documentId]/{page,loading,error}.js`, `app/api/portal/knowledge/{upload,search}/route.js`, `app/api/portal/knowledge/[documentId]/{route.js,reindex/route.js}`, `components/portal/{KnowledgeCard,KnowledgeListView,KnowledgeSearch,UploadKnowledgeForm,DocumentActions,KnowledgeAssignedWorkers}.js` all present. 59 unit tests passing (14 new). End-to-end smoke-tested against a live backend as both admin and non-admin; found and characterized a real backend defect (see §5 item 5). |
| **Package 5 — Chat** | ✅ Complete, validated | `lib/api/chat.js` (4 functions), `app/portal/(protected)/chat/{page,loading,error}.js`, `app/portal/(protected)/chat/[workerId]/{page,loading,error}.js`, `app/portal/(protected)/chat/[workerId]/[sessionId]/{page,loading,error}.js`, `app/api/portal/chat/route.js`, `app/api/portal/chat/[workerId]/sessions/route.js`, `components/portal/{ChatWorkerCard,ChatThread,ChatComposer,ChatSessionStarter,ChatInterface}.js` all present. 66 unit tests passing (7 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both admin and non-admin; found a significant backend behaviour (see §5 item 6). |
| **Package 6 — Memory** | ✅ Complete, validated | `lib/api/memory.js` (2 functions), `app/portal/(protected)/memory/{page,loading,error}.js`, `app/portal/(protected)/memory/[workerId]/{page,loading,error}.js`, `app/portal/(protected)/memory/[workerId]/[memoryId]/{page,loading,error}.js`, `app/api/portal/memory/route.js`, `components/portal/{MemoryListItem,MemoryOverviewView,AddMemoryForm}.js` all present, plus a cross-link added to Package 3's worker detail page. 71 unit tests passing (5 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both admin and non-admin, including a genuine auto-memory capture (see §5 items 6–7). |
| **Package 7 — Administration** | ✅ Complete, validated | `lib/api/admin.js` (4 functions), `app/portal/(protected)/admin/layout.js` (role gate), `app/portal/(protected)/admin/{page,loading,error}.js`, `app/portal/(protected)/admin/users/{page,loading,error}.js`, `app/portal/(protected)/admin/organisation/{page,loading,error}.js`, `app/portal/(protected)/admin/permissions/{page,loading,error}.js`, `app/api/portal/admin/{users,permissions}/route.js`, `components/portal/{CreateUserForm,UserListView,PermissionMatrix}.js` all present, plus a conditional "Admin" nav link. 81 unit tests passing (10 new). End-to-end smoke-tested against a live backend as both admin and non-admin; found and live-reproduced a real backend defect via a direct Postgres check (see §5 item 8). |
| **Package 8 — Knowledge connectors "coming soon"** | ✅ Complete, validated | `lib/api/connectors.js` (1 function), `app/portal/(protected)/knowledge/connectors/{page,loading,error}.js`, `components/portal/ConnectorCard.js` all present, plus a "Connectors" button added to the existing `/portal/knowledge` page (no new top-level nav entry — this is a Knowledge sub-page). 82 unit tests passing (1 new). End-to-end smoke-tested against a live backend as both admin and non-admin; confirmed connector responses are identical across accounts/organisations and that the backend's connector classes are unreachable dead code (see §5 item 11). |
| **Package 9 — Billing & Licensing** | 🟡 Frontend UX scaffold complete, validated — **not a real *commercial billing* system** | `lib/licensing/referenceLicence.js` (reference/demo data, not a backend wrapper), `app/portal/(protected)/admin/billing/*` (layout, 7 sub-pages), `components/portal/{WizardShell,BillingNav,CapacityMeter,RenewalWizard,WorkerPackWizard,OwnershipTransferWizard}.js` all present. 90 unit tests passing (8 new). End-to-end smoke-tested against a live backend as both admin and non-admin; **found and fixed a real information-exposure bug** (a non-admin's raw response contained an unreferenced, real-data copy of admin-gated content — see §5 item 12). At the time this row was verified (2026-08-15), `teracom-ai-backend` had zero billing/licensing support of any kind — confirmed by reading every model and every one of the 39 registered routers then in `main.py`. **This finding is now stale as a *current* statement** — backend Phase 0 Packages A–D shipped afterward with a real `Licence`/`LicenceRequest`/`Entitlement`/`HardwareFingerprint`/`LicensingAuditLog` issuance-and-tier-gating mechanism, discovered and corrected during Package M's own research (see §3's note and ADR-017). What genuinely remains absent, unchanged from this row's original finding, is the *commercial billing layer* specifically — price, invoicing, payment, a `Subscription` entity. See [[commercial-model]], [[licensing-model-v1]] (supersedes the original [[licensing-model]] — see [[licensing-changelog]]), and [[product-editions]] for the commercial design this scaffold represents but does not yet implement. |

| **Package G — Autonomous CTO & Organisational Intelligence** | ✅ Complete, validated | `lib/api/ctoOrchestration.js` (3 functions), `app/portal/(protected)/cto/page.js`, `app/api/portal/cto/{plan,execute}/route.js`, `components/portal/{CtoOrchestrationPanel,CtoExecutionHistory}.js` all present. 116 unit tests passing (12 new). End-to-end smoke-tested against a live backend and a real local Ollama instance, including genuine multi-hop delegation chains. See `PHASE_0_PACKAGE_G_AUTONOMOUS_CTO_IMPLEMENTATION_REPORT.md` (backend-repo report, filed under `docs/backend/` in this repo). This row was missing from this table until Package H's own verification pass added it — the package itself was real and complete since 2026-08-16, this file simply hadn't been updated. |
| **Package H — Knowledge & Memory Intelligence** | ✅ Complete, validated | `lib/api/{departments,organisationMemory,departmentMemory,memorySummaries}.js`, `app/portal/(protected)/admin/departments/*`, `app/portal/(protected)/memory/{organisation,department/[departmentId]}/*`, five new `/api/portal/*` BFF routes, `components/portal/{CreateDepartmentForm,DepartmentListView,AssignWorkerDepartmentControl,OrganisationMemoryView,AddOrganisationMemoryForm,DepartmentMemoryView,AddDepartmentMemoryForm,MemorySummaryPanel}.js` all present. 135 unit tests passing (19 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both an admin and a `member`, including a real CTO chain confirming organisation-tier memory is included in an admin-triggered chain's synthesis and correctly excluded from a member-triggered one. See `MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md`. |
| **Package I — Department Head Layer & Executive Organisation** | ✅ Complete, validated | `lib/api/departmentHeads.js`, `app/portal/(protected)/departments/{page,[departmentId]/page}.js`, two new `/api/portal/*` BFF routes, `components/portal/{DepartmentDashboard,DepartmentHeadConsultationPanel,AssignDepartmentHeadControl}.js` all present, plus additive changes to `DepartmentListView`/`CtoOrchestrationPanel`/`CtoExecutionHistory`/`PortalNav`. 143 unit tests passing (8 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both an admin and a `member`, including a real CTO chain routing a "sales pipeline" objective to a "Head of Sales" department head, a real department-head-to-department-head consultation, and a real, reproduced difference between a department head's and an ordinary department member's chat answer to the identical factual question (the head correctly used department memory; the non-head hallucinated). See `DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md`. |
| **Package J — Sales & Customer Success Platform** | ✅ Complete, validated | `lib/api/{crm,dealDocuments,onboardingTasks}.js`, `app/portal/(protected)/{sales,sales/[contactId],customer-success}/*`, ten new `/api/portal/*` BFF routes, `components/portal/{ContactIntakeForm,ContactListView,ContactDetail,DealDocumentPanel,OnboardingChecklist,PipelineSummaryWidget,CustomerHealthWidget,DepartmentFunctionControl}.js` all present, plus additive changes to `DepartmentDashboard`/`DepartmentListView`/`PortalNav`. 177 unit tests passing (34 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both an admin and a `member`, including a full contact lifecycle (prospect → lead → AI-drafted-and-approved proposal → approved quote → approved contract → customer → onboarding) with every proposal/quote/contract decision correctly gated to admin-only, verified through both the direct backend API and this frontend's own BFF routes. See `SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md`. |
| **Package K — Marketing & Media Platform** | ✅ Complete, validated | `lib/api/{marketing,marketingProduction,mediaCentre}.js`, `app/portal/(protected)/{marketing,marketing/[campaignId],media-centre}/*`, twelve new `/api/portal/*` BFF routes, `components/portal/{CampaignForm,CampaignListView,CampaignDetail,ContentPiecePanel,VideoAssetPanel,MediaCentreView,MarketingSummaryWidget}.js` all present, plus additive changes to `DepartmentDashboard`/`DepartmentFunctionControl`/`PortalNav`/`/portal/cto`. 204 unit tests passing (27 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both an admin and a `member`, including a full campaign lifecycle (campaign created → AI-drafted-and-approved content → AI-drafted-and-approved video script informed by that approved content → published to the Media Centre → marked published) with every content/video decision and the Media Centre's mark-published action correctly gated to admin-only, verified through both the direct backend API and this frontend's own BFF routes. See `MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md`. |
| **Package L — Federation Registry & External AI Consulting** | ✅ Complete, validated | `lib/api/{federation,federationConsultation,organisations}.js`, `app/portal/(protected)/federation/*`, three new `/api/portal/*` BFF routes, `components/portal/{FederationProviderList,FederationConsultationPanel,FederationConsultationHistory,FederationSummaryWidget,FederationEnabledToggle}.js` all present, plus additive changes to `PortalNav`/`/portal/cto`/`/portal/admin/organisation`. 215 unit tests passing (11 new). End-to-end smoke-tested against a live backend and a real local Ollama instance as both an admin and a `member`, including a real confidence check on a narrow-purpose worker (confidence 0.00, correctly suggesting escalation), a real Ollama-generated "federation response" from the best-matched provider (clearly marked `is_simulated: true`, with a populated estimated token/cost), and the `federation_enabled` governance toggle correctly disabling the entire flow even on a Platinum-tier organisation, verified through both the direct backend API and this frontend's own BFF routes. See `FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md`. |
| **Package M — CFO & Finance Platform** | ✅ Complete, validated | `lib/api/{finance,departmentBudgets}.js`, additive changes to `lib/api/dealDocuments.js`, `app/portal/(protected)/finance/*`, three new `/api/portal/*` BFF routes, `components/portal/{FinanceSummaryWidget,DepartmentBudgetPanel,LicensingSummaryCard}.js` all present, plus additive changes to `DealDocumentPanel`/`DepartmentFunctionControl`/`DepartmentDashboard`/`PortalNav`/`/portal/cto`/`/portal/departments/[departmentId]`. 224 unit tests passing (9 new). End-to-end smoke-tested against a live backend as both an admin and a `member` (no Ollama call needed — nothing in this package's own new code touches it), including a department budget correctly gated to admin-only decide, a `member` freely setting a proposal's internal cost estimate (no gate — not a pricing decision), and `GET /finance/summary` correctly rolling up the approved budget, the cost estimate, zero federation cost, and this organisation's real, granted Enterprise licence/entitlement data, verified through both the direct backend API and this frontend's own BFF routes. See `CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md`. |
| **Package N — Operations & Project Delivery Platform** | ✅ Complete, validated | `lib/api/{operations,projects,tasks}.js`, `app/portal/(protected)/operations/*`, four new `/api/portal/*` BFF routes, `components/portal/{OperationsSummaryWidget,ProjectPanel,TaskPanel}.js` all present, plus additive changes to `DepartmentFunctionControl`/`DepartmentDashboard`/`PortalNav`/`/portal/cto`/`/portal/departments/[departmentId]`. 231 unit tests passing (7 new). End-to-end smoke-tested against a live backend as both an admin and a `member` (no Ollama call needed — nothing in this package's own new code touches it), including a `member` freely creating a project and task and progressing a task through every status with no 403 anywhere (confirming the ungated design), and `GET /operations/summary` correctly rolling up project/task status counts and the overdue calculation, verified through both the direct backend API and this frontend's own BFF routes. See `OPERATIONS_AND_PROJECT_DELIVERY_IMPLEMENTATION_REPORT.md`. |
| **Package O — Customer Portal & Self-Service Platform** | ✅ Complete, validated | `lib/api/portalContact{Auth,Dashboard,Deals,Onboarding,Projects,Communications,Knowledge,SupportRequests,Accounts}.js`, `lib/api/supportRequests.js`, a full new `app/customer-portal/**` route tree (its own login/session/nav), `app/portal/(protected)/support/*`, six new BFF routes, `components/customer-portal/*` (8 files), `components/portal/{PortalAccountPanel,SupportRequestPanel}.js` all present, plus additive changes to `ContactDetailPage`/`CustomerHealthWidget`/`PortalNav`/`middleware.js`. 258 unit tests passing (16 new — cookie-touching auth helpers intentionally left untested by `node:test`, same restraint as the pre-existing `lib/api/auth.js`, since `next/headers` isn't resolvable outside the Next.js runtime). End-to-end smoke-tested against a live backend as an admin, a portal contact, and a second isolated portal contact (no Ollama call needed — nothing in this package's own new code touches it), including a portal contact's login only ever resolving to its own `crm_contact_id`'s data, `internal_cost_estimate` genuinely absent from every portal-facing proposal payload, an incident report automatically producing a real `Task` under an auto-created "Customer Incidents" `Project` visible in `/portal/operations`, a two-directional support-request message thread, and `GET /crm/pipeline-summary`'s new portal-adoption/engagement fields, verified through both the direct backend API and this frontend's own BFF routes. See `CUSTOMER_PORTAL_AND_SELF_SERVICE_IMPLEMENTATION_REPORT.md`. |
| **Package PQR — Worker Lifecycle & Governance + Architecture Integration + Production Operations** | ✅ Complete, validated | `lib/api/{workerCreationRequests,governancePolicies,deploymentRecords,platformIncidents,platformHealth}.js`, additive changes to `lib/api/workers.js`, `app/portal/(protected)/{workers/requests,admin/governance,platform-health}/*`, seven new `/api/portal/*` BFF routes, `components/portal/{WorkerCreationRequestPanel,GovernancePolicyTable,DeploymentRecordPanel,PlatformIncidentPanel,PlatformHealthSummaryWidget}.js` all present, plus additive changes to `WorkerListView`/`PortalNav`/`/portal/cto`. 279 unit tests passing (21 new). End-to-end smoke-tested against a live backend as both an admin and a `member` (no Ollama call needed anywhere in this package's own verification; a real `POST /cto/plan` call — deterministic decomposition/routing, not Ollama-backed — confirmed the `Department.function` routing fix live), including a `member` submitting a worker creation request that only an admin could decide, approval creating a real Worker, a two-step deployment submit→decide→complete gate rejecting a premature complete, ungated platform incident reporting, and `GET /platform-health/summary`'s computed status correctly reflecting real incident/deployment data, verified through both the direct backend API and this frontend's own BFF routes. See `WORKER_GOVERNANCE_ARCHITECTURE_AND_PRODUCTION_OPERATIONS_IMPLEMENTATION_REPORT.md`. |
| **Package Q — Commercial Provisioning & Entitlements** | ✅ Complete, validated | `lib/api/{licensing,workerPackProvisioning,organisationOnboarding}.js`, 3 new `/api/portal/*` BFF routes, `app/portal/(protected)/onboarding/*`, `components/portal/{OrganisationOnboardingChecklist,WorkerPackProvisionAction}.js` all present, plus real-submission changes to `WizardShell.js`/`WorkerPackWizard.js` and a "Provision this pack" action added to `/portal/marketplace/[slug]`. 294 unit tests passing (7 new). End-to-end smoke-tested against a live backend and a live frontend (no Ollama needed), including a brand-new organisation's `initial_issuance` request being approved and immediately producing a real entitlement and a real 5-item welcome checklist, a real Marketplace pack being provisioned into 2 real `Worker` rows each traceable back to the same `WorkerPackProvisioning` record, and both the onboarding checklist and the pack detail page rendering that real data through the actual Next.js BFF proxy chain (not `TestClient`, not a mock). See `PHASE_0_PACKAGE_Q_COMMERCIAL_PROVISIONING_AND_ENTITLEMENTS_IMPLEMENTATION_REPORT.md`. |

| **Platform Review Wave 1 — Teracom UX, Branding and Navigation Refresh** | ✅ Complete, validated | `components/portal/{PortalNav,icons,AiConciergePlaceholder,ForgotPasswordForm,ResetPasswordForm,TrialSignupForm,SystemMetricsPanel}.js`, `lib/api/{systemMetrics}.js` + 3 new functions on `lib/api/auth.js`, `app/portal/(public)/{forgot-password,reset-password,start-trial}/*`, 3 new `/api/auth/*` BFF routes, `middleware.js` (public-path allowlist fix), additive changes to `StatTile.js`/`OrganisationSummaryCard.js`/dashboard/login/platform-health pages all present. 295 unit tests passing (1 new file). End-to-end smoke-tested against a live backend and a live frontend on non-conflicting ports (the project owner's own port 8000/3000 sessions were left untouched throughout, verified by exact-PID/port mapping before stopping anything); found and fixed a real bug (three new public pages 307'd to login before the middleware allowlist fix). See `PLATFORM_REVIEW_WAVE_1_UX_BRANDING_AND_NAVIGATION_REFRESH_IMPLEMENTATION_REPORT.md`. |
| **Package CX1 — Customer Experience & Commercial Readiness Wave** | ✅ Complete, validated | Backend: `EmailMessage`/`NotificationLog`/`PortalContactPasswordResetToken`/`Lead` models, `services/{email,notification,trial,portal_contact_password_reset,lead}_service.py`, `api/{leads,staff_leads,staff_notifications}.py`, new `PortalContact` forgot/reset-password routes, trial lifecycle wired into `auth.py#me()`/`workers.py#create_worker()`/`worker_pack_provisioning_service.py`, migration `2f5a8c1d9e33` applied to the real dev database. Frontend: `lib/api/{portalContactAuth,leads}.js`, `app/customer-portal/(public)/{forgot-password,reset-password}/*`, `components/customer-portal/PortalContact{ForgotPassword,ResetPassword}Form.js`, a rewritten real `app/api/leads/route.js`, a rewritten interactive `AiConciergePlaceholder.js`, `middleware.js` (customer-portal public-path allowlist), homepage lead-banner/deep-link support, `OrganisationSummaryCard.js` reading the backend-computed `trial_status`. Backend: 222/222 tests passing. Frontend: lint clean, 296/296 tests passing, clean build. Live-verified end to end against the real running services (trial signup → grace → locked → worker-limit; `PortalContact` forgot/reset password full round trip; leads capture via the real contact form; the AI Concierge widget; staff notifications/leads visibility), all verification data cleaned from the database afterward. Two real bugs found and fixed via full-suite testing (a forgotten notification hook, a shared rate-limiter instance across identity planes — see ADR-024) and a mid-wave production incident (stale frontend build) fully root-caused and fixed. Committed and pushed: backend `d81eeb8`, frontend `534f758`. See `PACKAGE_CX1_CUSTOMER_EXPERIENCE_AND_COMMERCIAL_READINESS_IMPLEMENTATION_REPORT.md` and ADR-024. |
| **Platform Review Wave 3 — Digital Workspace Completion** | ✅ Complete, validated | Frontend-only, no new dependencies. `PortalNav.js` (dropdown-close bug, full WAI-ARIA menu-button pattern), `CustomerPortalNav.js` (new brand link), `icons.js` (5 new icons: `BillingIcon`/`PermissionIcon`/`MegaphoneIcon`/`FunnelIcon`/`PulseIcon`, `StatusDot` generalised and finally wired into `WorkerCard.js`/`ChatWorkerCard.js`), `CapacityMeter.js` (new optional `icon` prop), a new `DashboardQuickLinks.js` component, `app/portal/(protected)/page.js` retired to a `redirect()` to `/portal/dashboard` (`AccountSummary.js` deleted as orphaned), every `StatTile`/`CapacityMeter`/executive-summary-widget call site across ~20 files now carrying an icon, the last three admin pages (`admin/{users,organisation,permissions}/page.js`) gaining the inline role check every other admin page already had, two leftover "CTO" branding strings fixed on the Orchestration page, 11 customer-portal page titles standardised, and a Marketplace product-family section matching the marketing homepage's own. Lint clean, 296/296 tests passing (unchanged), clean build, live-verified against the real running services with a throwaway organisation created and fully deleted afterward. Built directly on `TERACOM_REVIEW_BACKLOG.md`'s own Wave-1 findings — see that file's own updated entries. Committed and pushed: frontend `9c67907`. See `PACKAGE_WAVE3_DIGITAL_WORKSPACE_COMPLETION_IMPLEMENTATION_REPORT.md` and ADR-025. |
| **Package SEC1 — Platform Security Hardening** | ✅ Complete, validated | Backend: `RefreshToken`/`StaffRefreshToken`/`PortalContactRefreshToken` models plus a `tokens_invalid_before` column on `User`/`StaffUser`/`PortalContact` (migration `9c3f7b21e5a4`), short-lived (15 min) access tokens with a genuine revocable 30-day refresh token per plane, real immediate logout, `CORSMiddleware`, `/docs`/`/redoc`/`/openapi.json` gated behind a new `ENVIRONMENT` config flag, four API-surface role-gating fixes. Frontend: a new refresh-token cookie, real backend-calling logout routes, a middleware-based silent-refresh mechanism, and a new Edge-runtime-safe `lib/api/edgeJwt.js`. Also: `.env` file permissions hardened (`600`), a new secret-rotation runbook section, and a real (prepared, not deployed) nginx/TLS config plus certbot runbook. Backend: 234/235 tests passing (13 new; the one failure is the same pre-existing Ollama-response-timing flake, confirmed passing in isolation). Frontend: lint clean, 300/300 tests passing (4 new), clean build. Live-verified end to end, including minting a real near-expired access token to prove the middleware silent-refresh path, and confirming a real logout immediately invalidates both the access token and its refresh token. A real configuration-drift bug (a stale `ACCESS_TOKEN_EXPIRE_MINUTES=60` override) was found and fixed during live verification. Committed and pushed: backend `85eac8a`, frontend `3725f25`. See `PACKAGE_SEC1_PLATFORM_SECURITY_HARDENING_IMPLEMENTATION_REPORT.md`, ADR-026, and `docs/operations/REVERSE_PROXY_AND_TLS.md`. |
| **Package EMAIL1 — Communications and Notification Framework** | ✅ Complete, validated — committed and pushed (backend `d9a70c9`, frontend `c9de840`); this row previously read "held uncommitted, pending approval," a stale claim corrected here | Backend: a real, generic `SmtpEmailProvider` (stdlib `smtplib` only) alongside the pre-existing `LoggingEmailProvider` fallback (`services/email_provider.py`), real HTML+text templates for all eleven notification types (`services/email_templates.py`), real delivery/audit tracking on `EmailMessage` (`delivered_at`/`error_message`/`provider` columns, migration `4d8a2f61c7b3`), a proactive trial-expiring-soon notice (new `Organisation.trial_expiring_soon_notified_at` column), Contact Sales/Demo Request lead notifications (new `SALES_NOTIFICATION_EMAIL` config), and onboarding notices for both `OrganisationOnboardingTask` (Package Q) and `OnboardingTask`/Customer Success (Package J, emailing a `CrmContact` directly for the first time). Two new endpoints (`GET /organisation-notifications/`, `GET /crm/contacts/{id}/communications`). Frontend: a new admin-only `/portal/admin/communications` page and a "Communications" section on the Sales contact detail page, both using a new shared `CommunicationsPanel.js`. Backend: 254/254 tests passing (19 new). Frontend: lint clean, 302/302 tests passing (2 new), clean build. Live-verified end to end against the real running services, including the full lead-notification pipeline with a real configured recipient and the Customer Success onboarding notice rendering live on the real contact detail page; this environment has no real SMTP credentials configured, so every live send used the honest logging fallback, not a fabricated "sent" claim. See `PACKAGE_EMAIL1_COMMUNICATIONS_AND_NOTIFICATION_FRAMEWORK_IMPLEMENTATION_REPORT.md` and ADR-027. |

| **Wave 1 (5 workstreams: Internal Pilot Blockers, SMTP Completion, Password Reset Completion, META1 - Metadata Foundation, Licensing and Entitlement Architecture)** | ✅ Complete, validated | Sourced from `teracom-ai-docs`' `IMPLEMENTATION_PLAN.md`, itself derived from `INTERNAL_PILOT_READINESS_ASSESSMENT.md`/`KNOW1_ASSESSMENT.md`/the amended `COMMERCIAL_READINESS_ASSESSMENT.md`. **Workstream 1:** fixed `DELETE /documents/{id}`'s unhandled `IntegrityError` (a new `Knowledge.permissions` cascade relationship, no migration needed, plus a new `chroma_service.delete_embedding()` closing a second, independent vector-store leak) and a previously-unrecognised cross-plane login rate-limiter bug (two new dedicated instances, mirroring ADR-024's own template). **Workstream 2:** confirmed `SmtpEmailProvider` needed no code change; added a real TCP/SMTP wire-level test (a minimal stdlib-only fake server, no new dependency) and a full activation runbook (`docs/operations/PRODUCTION_RUNBOOK.md` §9) — real credentials remain unconfigured, requiring access outside this session's scope. **Workstream 3:** fixed a real defect independent of Workstream 2 — the reset token was generated and immediately discarded on both identity planes, so the reset email never contained a working link regardless of SMTP status; both password-reset services now build a real URL (new `FRONTEND_BASE_URL` config) and thread it through a rewritten email template. **Workstream 4 (META1):** a new Knowledge classification layer (`document_type`/`sensitivity_level` columns, a real indexed `knowledge_tags` join table, a new `services/metadata_service.py`, new `GET`/`PATCH /knowledge/{id}/metadata` endpoints), built ahead of the Knowledge Graph/Document Lifecycle Governance/Organisational Search packages it exists to serve. **Workstream 5:** a new `Plan` entity (seeded to mirror `entitlement_service.py`'s hardcoded tier limits exactly) and an additive `Licence.plan_id` (populated for new licences, backfilled for existing ones via migration `9d4b6f1e2c7a`), plus a `LicenceBillingReference` side table prepared for a future billing integration — deliberately an expand-only migration; `Licence.tier` and all 13 confirmed enforcement call sites are unchanged, with the "contract" step (switching reads, dropping `tier`) named as distinct future work. New migrations: `7a2c9e4f1b8d`, `9d4b6f1e2c7a`. Backend: 277/277 tests passing (23 new across the wave, up from 254/254 at wave start). Frontend: no code changes across any of the five workstreams — 302/302 passing throughout, unchanged. Five new ADRs (028-032). See `WAVE1_COMPLETION_REPORT.md` for the full wave retrospective and each workstream's own implementation report. |

Full technical detail for what's built: [[frontend-architecture]], and the implementation reports under `docs/frontend/IMPLEMENTATION_REPORTS/`. Current in-flight work: [[current-sprint]].

## 3. Backend state — mostly as documented; Knowledge's, Chat's, Memory's, Administration's, and Connectors' endpoints independently verified; commercial billing confirmed to not exist, but real licensing/entitlement infrastructure does (corrected 2026-08-17, Package M)

The backend repository is not part of this git repository, but a checkout of it is available on disk in this environment (sibling directory `teracom-ai-backend`) and was read directly while building Package 4 (Knowledge), Package 5 (Chat), Package 6 (Memory), Package 7 (Administration), Package 8 (Connectors), and Package 9 (Billing & Licensing) — `api/knowledge.py`, `api/documents.py`, `api/upload.py`, `api/search.py`, `api/chat.py`, `api/chat_sessions.py`, `api/conversation_summary.py`, `api/memory.py`, `api/memory_summary.py`, `api/users.py`, `api/organisations.py`, `api/permissions.py`, `api/connectors.py`, `api/connector_status.py`, `services/memory_service.py`, `services/auto_memory_service.py`, `services/worker_knowledge_service.py`, `services/connector_status_service.py`, `services/connectors/*.py`, and their schemas were confirmed against source, not taken secondhand from the architecture doc. For Package 9 specifically, the verification was a targeted, whole-repository grep (`licen|billing|plan|subscription|stripe|seat` — zero matches) plus reading every field on `models/organisation.py`/`models/user.py` and every one of the 39 routers in `main.py` **as they stood on 2026-08-15** — the finding *was* the absence, at that time. **Correction (2026-08-17, discovered during Package M's own research):** this is no longer an accurate statement of current backend capability. Backend Phase 0 Packages A–D shipped `models/{licence,licence_request,entitlement,hardware_fingerprint,licensing_audit_log}.py` and `api/{licensing,staff_licence_requests}.py` sometime after Package 9's own verification — a real, complete licence-issuance/entitlement/tier-gating mechanism (submit → Teracom-staff-decide → signed `Licence` + `Entitlement` snapshot → `capability_allowed_for_tier()` gating) that every package since Package G has exercised via `_grant_active_licence()` in its own e2e tests. What genuinely remains absent, confirmed by the same exhaustive-grep method (no `price`/`cost`/`amount`/`currency`/`dollar` field on any of those five models): the *commercial billing layer* — invoicing, payment processing, seat/usage-based pricing, and a `Subscription` entity (`models/licence.py`'s own docstring: "Subscriptions is a later, separate package"). Package M's own `services/finance_summary_service.py#_get_licensing_summary()` is the first frontend-facing surface built on this real data — see `PHASE_0_PACKAGE_M_CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md` and ADR-017. Everything about Packages 1–3's backend surface, and anything not listed below, still comes from `FRONTEND_ARCHITECTURE_V1.md` Part B (a review conducted when that document was written) plus references inside the implementation reports to a `FINAL_SECURITY_REMEDIATION.md` file that lives in the backend repo. See [[backend-status]] for the full summary and [[remediation-history]] for the security-hardening history as documented secondhand.

Headline facts worth keeping in working memory:

- Multi-tenant isolation (`organisation_id` scoping) is implemented and consistently enforced — solid.
- ~~No CORS middleware, no refresh tokens, no logout/revocation endpoint~~ — **fixed, Package SEC1 (2026-08-18).** Real `CORSMiddleware`, a genuine per-plane refresh token, and real immediate logout (`tokens_invalid_before` + `iat` check) all now exist across all three identity planes. Single-string role checks (`role` has no server-side enum) and no commercial billing/pricing/subscription data model at all remain true (real `Licence`/`Entitlement` tier-gating data does exist, since backend Packages A–D — see §3's correction above).
- Five near-duplicate dashboard aggregate endpoints exist; the frontend has standardised on `GET /portal-dashboard/` as canonical (§C.6 of the architecture doc, confirmed followed in Package 2's implementation).
- Connectors (SharePoint/OneDrive/Teams) are hardcoded stubs, not real integrations.
- ~~`DELETE /documents/{id}` and `POST /documents/reindex/{id}` have no role check at all.~~ **Fixed, Package SEC1** — both are now admin-only. `POST /upload/` was reviewed and deliberately left member-accessible (a create/ingest action, not a destructive one) — see the SEC1 implementation report §2.
- `POST /upload/`'s response (`{filename, status}`) never includes the created document's id — verified against `schemas/upload.py`. Anything building a post-upload redirect must account for this.
- ~~**Real, live-reproduced defect:** `DELETE /documents/{id}` throws an unhandled `IntegrityError` (bare 500) when the document still has a `knowledge_permissions` row referencing it.~~ **Fixed, Wave 1 — Internal Pilot Blockers (2026-08-19).** `models/knowledge.py`'s new `Knowledge.permissions` relationship (`cascade="all, delete-orphan"`) removes dependent `KnowledgePermission` rows in the same transaction, no migration required; `services/chroma_service.py`'s new `delete_embedding()` also cleans up the corresponding vector-store entry (best-effort, logged on failure) so a deleted document is no longer returned by semantic search either. See `WAVE1_INTERNAL_PILOT_BLOCKERS_IMPLEMENTATION_REPORT.md`.
- **`POST /chat/` silently creates a brand-new `ChatSession` on every single call and never returns its id** — verified directly against `services/chat_persistence_service.py#persist_chat()` (calls `create_session()` unconditionally) and confirmed live by sending a real chat message, then querying Postgres directly and finding the auto-created session with the right two messages, unreachable from the app itself. `POST /chat-sessions/{worker_id}` creates a *separate*, always-empty session with a real id — the two mechanisms don't connect. No specific conversation can be resumed or looked up from this frontend as a result. See `CHAT_IMPLEMENTATION_REPORT.md` §2 for the full mechanism.
- Ollama is real and working in this environment (`llama3`/`llama3.1` models present, `stream: False` hardcoded in `services/ollama_service.py` — confirmed no streaming exists), and a real completion was exercised end-to-end during Package 5's smoke test, correctly reflecting a worker's own configured instructions.
- **Neither `POST /memory/store` nor `GET /memory/{worker_id}`/`GET /memory-summary/` has any role check** (only `get_current_user` + worker-ownership) — verified directly against `api/memory.py`, and confirmed live with a non-admin account successfully adding a memory.
- **There is no update or delete endpoint for a memory anywhere in the backend** — only create and read exist. Verified by reading every route in `api/memory.py` and grepping the whole backend for `WorkerMemory` usage.
- **`services/auto_memory_service.py`'s trigger list is exactly 7 fixed, case-insensitive substring phrases** ("my preferred", "i prefer", "our preferred", "my vendor", "our vendor", "head office", "our office"), and it **stores the entire raw chat message verbatim** as the memory's content, not an extracted fact — verified both from source and live, by sending a real trigger-phrase message and confirming the resulting memory's content was the exact sentence sent.
- **`GET /users/` is admin-gated, not just `POST /users/`** — a real, backend-enforced boundary (unlike most list endpoints in this app), verified directly against `api/users.py`. `GET /organisations/` is likewise admin-gated and returns only `{id, name, slug}` — no plan/seat/billing field exists on the `organisations` table, confirming yet again there is no billing data model. `GET /permissions/` has no role check at all, asymmetric with the admin-only `POST /permissions/` on the same router — verified directly against `api/permissions.py`.
- **Real, live-reproduced defect:** `POST /permissions/` has no deduplication check, unlike `POST /worker-knowledge/assign`'s `assign_knowledge()` (which returns the existing row if one already exists) — calling it twice for the identical `(worker_id, knowledge_id)` pair creates two separate `KnowledgePermission` rows, confirmed directly in Postgres. A verified consequence: since the only delete path (`remove_knowledge()`) removes just one matching row, a single "Remove" click on a duplicated grant left one row — and the worker's access — intact. See `ADMIN_IMPLEMENTATION_REPORT.md` §2/§8/§10 for the full repro and the fix needed.
- **No user-role-change, deactivation, or delete endpoint exists** — confirmed by reading every route in `api/users.py`: create and list only.
- **Every connector endpoint (`GET /connector-status/`, `GET /connectors/{sharepoint,onedrive,teams}`) returns a hardcoded literal with no role check and no organisation-scoping at all** — verified directly against `api/connectors.py`/`api/connector_status.py`, and confirmed live: an admin and a non-admin account in the same freshly-created organisation both received the byte-for-byte identical response. `services/connectors/*.py`'s `SharePointConnector`/`OneDriveConnector`/`TeamsConnector` classes define real-looking `connect()`/`sync()` methods but are never instantiated or imported anywhere outside their own directory (confirmed by grepping the whole backend) — fully unreachable dead code. Even `BaseConnector.status()` (never overridden) would honestly report `"not_implemented"`, directly contradicting the "available" the live hardcoded endpoints claim.
- **Zero billing/licensing backend support exists at all — not even a stub.** Confirmed by grepping the entire backend for "licen|billing|plan|subscription|stripe|seat" (zero matches outside this frontend's own docs) and reading every field on `organisations`/`users` (name/slug only; no plan/tier/hosting/seat field anywhere). This is a stronger finding than Connectors' stubs — there is nothing to call, not merely a fake response. See `BILLING_AND_LICENSING_IMPLEMENTATION_REPORT.md` §2/§3 for the full, itemised list of backend work this would need.
- **Package H (2026-08-17):** `departments`, `department_memories`, `organisation_memories`, and `memory_summaries` tables now exist, plus a nullable `workers.department_id`. Governance ladder verified directly, live: worker memory unchanged (any org member, no gate); department memory — any org member reads, admin-only writes; organisation memory — admin-only both ways. `services/entitlement_service.py`'s `"memory_enrichment"` capability (Enterprise+) — registered in Package B but never wired to any endpoint until now — gates every new memory-intelligence endpoint (not departments themselves, which are free like Workers/Knowledge). `services/cto_orchestration_service.py#execute_chain()` now includes organisation-tier memory in its executive synthesis only when the triggering user is an admin — verified live by reproducing a real difference in synthesis output between an admin-triggered and a member-triggered identical chain. See `PHASE_0_PACKAGE_H_MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md`.
- **Package I (2026-08-17):** `departments.head_worker_id` (nullable FK to `workers.id`) now exists — a Department Head is an existing Worker, not a new entity; a worker must already belong to a department before being designated its head (enforced service-side, 400 otherwise). `_pick_worker_for_subtask()` now routes a CTO subtask to a department's head when the *department's own* name/description best matches, with no departments/heads (every pre-Package-I organisation) leaving behaviour unchanged — verified live with a "sales pipeline" objective correctly routing to a "Head of Sales" worker whose own persona fields never mentioned sales. `services/context_builder.py#build_context()` now includes a department's memory in its *head's* ordinary chat context (gated by `memory_enrichment`) — verified live: the head correctly answered from department memory while a non-head member of the identical department hallucinated. A new `POST /department-heads/consult` restricts Package F's existing consult mechanism to current department heads only, recording to the same tables the generic endpoint already uses (verified live: the same row appears in both). See `PHASE_0_PACKAGE_I_DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md`.
- **Package J (2026-08-17):** `crm_contacts`, `proposals`, `quotes`, `contracts`, `onboarding_tasks`, and `crm_audit_log` tables now exist, plus a nullable `departments.function`. A `CrmContact` is tracked on one row for its whole life (`stage`: prospect → lead → customer, forward-only); `Proposal`/`Quote`/`Contract` are three separate tables, each with its own submit → **organisation-admin**-decide lifecycle (not Teracom staff, since this is the customer's own pipeline) — verified live: a `member` can submit any of the three but is `403`'d attempting to decide on one, through both the direct backend API and the frontend's own BFF routes. A new `sales_intelligence` capability (Enterprise+) gates only AI-drafted proposal content (`POST /proposals/draft`, a real Ollama call); quotes and contracts are always human-entered. A cosmetic CRM connector abstraction (`services/crm_connectors/`) mirrors Package 8's Connectors precedent exactly — never wired to anything real. See `PHASE_0_PACKAGE_J_SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md`.
- **Package K (2026-08-17):** `campaigns`, `content_pieces`, `video_assets`, `media_centre_items`, and `marketing_audit_log` tables now exist (no schema change needed for `departments.function`'s new `"marketing"` value — the column was already an unconstrained nullable string). A `Campaign` is the container for a genuinely new three-tier pipeline — Marketing Manager Worker (direction) → Content Producer Worker (execution) → Video Producer Worker (execution) — where `VideoAsset.content_piece_id` optionally chains from an *approved* `ContentPiece`, mirroring `Quote.proposal_id`'s shape; `ContentPiece`/`VideoAsset` share `Proposal`'s submit → **organisation-admin**-decide lifecycle. A new dedicated `MediaCentreItem` model (not an extension of Package 4's Knowledge/Document model) is created only from an approved content piece or video asset, and only advances `"ready"` → `"published"` via a further explicit admin-only action — verified live: a `member` can submit content/video and publish a ready Media Centre item but is `403`'d attempting to decide on content/video or mark an item published, through both the direct backend API and the frontend's own BFF routes. A new `marketing_intelligence` capability (Enterprise+) gates only AI-drafted content/script (`POST /content/draft`, `POST /videos/draft-script`, both real Ollama calls) — the video-script draft reads the linked, approved content piece's own text into its prompt, the concrete pipeline handoff. A cosmetic YouTube connector abstraction (`services/media_connectors/`) mirrors Package 8's/Package J's Connectors precedent exactly — never wired to anything real. See `PHASE_0_PACKAGE_K_MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md`.
- **Package L (2026-08-17):** `federation_providers` (a global, non-organisation-scoped registry seeded with three illustrative rows — OpenAI GPT-4, Anthropic Claude, Azure OpenAI, each with its own capability-tag catalogue and a simulated cost-per-1k-tokens rate), `federation_consultations`, and `federation_audit_log` tables now exist, plus a new `organisations.federation_enabled` boolean (server-default true) — the concrete governance-control deliverable, toggled via `PATCH /organisations/federation-enabled` (admin-only). A worker's confidence in answering a message is a deterministic keyword-overlap heuristic (`services/federation_service.py#compute_confidence()`, reusing `services/orchestration_service.py#significant_words()`) — not a model-generated score. `POST /federation/suggest` (free) → `POST /federation/consult` (the confirm action) mirrors the existing `/orchestration/suggest` → `/orchestration/consult` shape exactly; the "federation response" is always generated locally via a real Ollama call and persisted with `is_simulated=True` — no real external provider API exists anywhere in this backend. A new `federation_consultation` capability gates at Platinum, alongside `cto_orchestration`/`orchestration_intelligence` (the existing ceiling of `_TIER_ORDER`) — no new tier was introduced. Verified live: a `member` can suggest and consult federation (ungated by role), a real Ollama call produced a genuine, on-topic answer correctly labelled simulated with populated estimated tokens/cost, and disabling `federation_enabled` as admin blocked both `/suggest` and `/consult` even on a Platinum-tier organisation, through both the direct backend API and the frontend's own BFF routes. A cosmetic connector-stub layer (`services/federation_providers/`) mirrors Packages 8/J/K's own connector precedent exactly, but — unlike those packages — is never imported by the real consultation flow either; the actual "federation response" always comes from Ollama directly. See `PHASE_0_PACKAGE_L_FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md`.
- **Package M (2026-08-17):** `department_budgets` and `finance_audit_log` tables now exist, plus a nullable `proposals.internal_cost_estimate` column. `DepartmentBudget` mirrors `Proposal`/`Quote`/`Contract`'s submit → organisation-admin-decide shape exactly (Package J) — the concrete deliverable behind governance's "human approval required for all financial commitments," verified live: a `member` can submit a budget but is `403`'d deciding it, through both the direct backend API and the frontend's own BFF routes. `Proposal.internal_cost_estimate` is a single new nullable column, distinct from the pre-existing customer-facing `amount` — not a pricing decision, so settable by any org member with no gate, verified live. `services/finance_summary_service.py#get_finance_summary()` reuses `services/federation_summary_service.py#get_federation_summary()`'s existing `total_estimated_cost` aggregate unmodified (no change to Package L's own models/service) and surfaces the organisation's real, existing `Licence`/`Entitlement` data — verified live against a real granted Enterprise licence. No new tier-gated capability was added — nothing in this package is AI-generated. This package's own research also discovered and corrected a stale claim in this file — see §3's correction above and ADR-017. See `PHASE_0_PACKAGE_M_CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md`.
- **Package N (2026-08-17):** `projects`, `tasks`, and `operations_audit_log` tables now exist (no schema change needed for `departments.function`'s new `"operations"` value). Unlike `CrmContact`/`Campaign` (always organisation-wide, a gap flagged in two prior packages' own risk lists), `Project.department_id` is nullable — a project may optionally belong to one department, closing that flagged gap for this new entity. Project/Task creation and status changes carry **no approval gate** — this extends `OnboardingTask`'s existing precedent (create-and-progress, Package J) rather than Proposal/Quote/Contract/DepartmentBudget's submit → admin-decide shape, since project/task tracking is operational execution tracking, not a financial or contractual commitment — verified live: a `member` can create a project/task and progress it through every status with no 403 anywhere. `operations_audit_log` still records every `project_created`/`task_created`/`task_completed` event despite the ungated design, mirroring `FinanceAuditLog`'s own "audit even when ungated" discipline. Head of Operations Worker received a new operational doer, Operations Manager Worker (the Package J/M pattern), while Project Manager Worker's existing product-facing definition was instead retrofitted with real mechanics (the Package K pattern) — two different established patterns applied within one package, per which persona's documented history actually fit. No new tier-gated capability was added — nothing in this package is AI-generated. See `PHASE_0_PACKAGE_N_OPERATIONS_AND_PROJECT_DELIVERY_IMPLEMENTATION_REPORT.md` and ADR-018.
- **Package O (2026-08-17):** `portal_contacts`, `support_requests`, and `support_request_messages` tables now exist, plus nullable `projects.crm_contact_id`/`tasks.support_request_id`/`knowledge.customer_visible` columns, and `projects.created_by_user_id` loosened to nullable (for the one system-created "Customer Incidents" Project). `PortalContact` is this backend's first identity plane scoped narrower than an organisation — mirrors `staff_users`' mechanism (separate table, separate JWT `aud` claim on the same signing key, separate `get_current_portal_contact()` dependency) but solves the opposite scoping problem: a `PortalContact`'s JWT resolves to exactly one `crm_contact_id`, verified live with two isolated portal-contact logins each unable to reach the other's proposals/quotes/contracts/support-requests. `schemas/portal_contact_view.py`'s slim response schemas structurally exclude `internal_cost_estimate`/`decision_notes`/internal user ids — verified live by inspecting raw response JSON keys, not just values. `SupportRequest`/`SupportRequestMessage` serve support requests, incident reports, and the communications timeline as one unified model; an incident-type request automatically creates a Task under an auto-created "Customer Incidents" Project (`services/project_service.py#get_or_create_incident_project()`) — verified live, including that a second incident reuses the same Project rather than creating a new one. `services/crm_pipeline_service.py#get_pipeline_summary()` gained `portal_accounts_count`/`open_support_requests_count` fields (objective #13), extending the existing Package J dashboard rather than building a new one. No new tier-gated capability was added — nothing in this package is AI-generated. See `PHASE_0_PACKAGE_O_CUSTOMER_PORTAL_AND_SELF_SERVICE_IMPLEMENTATION_REPORT.md` and ADR-019.
- **Package PQR (2026-08-17):** `worker_creation_requests`, `worker_audit_log`, `deployment_records`, `platform_incidents`, and `platform_operations_audit_log` tables now exist; no changes to any existing table. `WorkerCreationRequest` is a second, optional path to a real Worker — submit → organisation-admin-decide, gating creation of the entity itself (approval creates a real `Worker` row from the stored fields; rejection creates none) — verified live: a `member` can submit but is `403`'d deciding, and the pre-existing direct `POST /workers/` admin creation is completely unchanged. `services/governance_policy_service.py`'s registry (mirroring `services/entitlement_service.py`'s shape) backs `GET /governance-policies/` (objective #7) — it is the actual enforcement mechanism for this package's own new admin-gated actions only; the 24 pre-existing `require_role("admin")` call sites elsewhere are documented as metadata, not retrofitted (a deliberate scope boundary, ADR-020). `_pick_worker_for_subtask()` (`services/cto_orchestration_service.py`) now also consults `department.function` via a static keyword-boost dictionary — closing the gap flagged since Package J, verified live with a real `POST /cto/plan` call (deterministic decomposition/routing — no Ollama involved) routing correctly to a sparsely-described `function`-tagged department, and via dedicated unit tests confirming byte-for-byte unchanged behaviour with no `function` tag. `DeploymentRecord` (submit → admin-decide → separate admin "complete", mirroring `MediaCentreItem`'s two-step gate) and `PlatformIncident` (ungated, mirroring `Task`) are organisation-scoped, not a new cross-tenant plane — reusing the existing Organisation/User model. `services/production_platform_health_service.py#get_platform_health_summary()` computes `status`/`open_incidents_by_severity`/`pending_deployments_count`/`last_deployment` live from real rows — named distinctly from the pre-existing `services/platform_health_service.py` (Package 7's own simple resource-count check backing `GET /health/`) after a genuine module-name collision was caught and fixed during implementation. No new tier-gated capability was added. See `PHASE_0_PACKAGE_PQR_WORKER_GOVERNANCE_ARCHITECTURE_AND_PRODUCTION_OPERATIONS_IMPLEMENTATION_REPORT.md` and ADR-020.
- **A Next.js App Router rendering gap: a layout that renders a different tree instead of `{children}` does not stop the child route's own Server Component (and its real data fetches) from executing.** Found live during Package 9's smoke test: a non-admin's raw HTTP response for three new billing pages contained an unreferenced, real-data copy of admin-gated content in the React Server Component flight payload, even though the visibly-rendered page correctly showed only `app/portal/(protected)/admin/layout.js`'s restricted message. Fixed for Package 9's own three affected pages (each now checks the role itself before fetching); very likely also affects Package 7's pre-existing `/portal/admin/{users,organisation,permissions}` pages the same way, not yet fixed there. See `BILLING_AND_LICENSING_IMPLEMENTATION_REPORT.md` §9.

## 4. Commercial state

Editions and worker catalogue are **approved but not yet implemented in any commercial billing system** — there is no `organisations.plan` column, no Stripe↔backend bridge, no price/invoicing/subscription data anywhere. (Correction, Package M: a real licence-issuance mechanism *does* exist — `Licence`/`LicenceRequest`/`Entitlement`, backend Packages A–D, complete with a signed artefact per `Licence.signed_payload_ref` — see §3's correction above; what's absent is specifically the commercial/pricing layer around it.) This is documented as the design target in [[commercial-model]], [[licensing-model-v1]] (approved 2026-08-15, ADR-011 — supersedes the original [[licensing-model]], which is marked superseded, not deleted; see [[licensing-changelog]]), [[pricing-model]], and [[product-editions]], and as the single longest-lead-time item in the roadmap ([[roadmap]] §Billing & Licensing). Package 9 has now built the full frontend UX this design target describes, against illustrative reference data (§2/§3) — the backend schema/endpoint work is the only thing separating that scaffold from a real feature, and is now the single blocking dependency for the rest of this roadmap (every other package has shipped). Separately, [[ux-vision]] (ADR-012) sets a Natural Language First / Wizard Second / Forms Last design-evaluation order — Package 9's three request wizards (renewal, worker pack, ownership transfer) are the first screens built directly against this rubric's Wizard tier, per §5 item 4's explicit instruction that human-approval-gated actions warrant a guided flow regardless of NL feasibility.

## 5. Open risks carried at all times

These are standing conditions, not one-time bugs — read them before making a decision that touches auth, billing, or connectors:

1. Frontend gating (role/plan checks in the UI) is presentation-only; the backend's `require_role` is the only real enforcement. **Partially updated, Package Q:** worker-limit (seat) enforcement is now real, but only for the new `POST /worker-pack-provisioning/` path — the pre-existing `POST /workers/` (direct admin creation) and `WorkerCreationRequest` approval remain unenforced, a deliberate scope boundary (see §5 item 40), not an oversight.
2. Sovereign Edition ([[product-editions]]) requires hardware-bound, offline-capable licensing that doesn't exist in any form today — no license-file format, no signing key infrastructure, no validation code. This is greenfield work, not an extension of anything currently built.
3. The backend's "recent activity" ordering is not actually chronological (no timestamp columns) — cosmetic today, will mislead if surfaced as history-of-record for compliance or Licensing & Compliance Worker consumption later.
4. **No worker-update or worker-delete endpoint exists backend-side.** `EditWorkerForm.js` is fully editable but deliberately non-persisting (an honest "saving isn't available yet" notice on submit) — see `WORKERS_IMPLEMENTATION_REPORT.md` §8. This is a backend gap to track, not a frontend defect.
5. ~~**`DELETE /documents/{id}` is broken for any document still assigned to a worker**~~ — **Fixed, Wave 1 — Internal Pilot Blockers (2026-08-19).** See §3's correction above and `WAVE1_INTERNAL_PILOT_BLOCKERS_IMPLEMENTATION_REPORT.md`.
6. **No chat conversation can be resumed or looked up by the user who had it.** `POST /chat/` creates a new session every call and never returns its id (see §3) — this is the single highest-value backend fix identified so far for the actual product experience, since "have an ongoing conversation with a worker" is a core expected capability of an AI chat product. Needs `ChatRequest`/`ChatResponse` to carry a `session_id` and `persist_chat()` to reuse an existing session when one is given, rather than always creating a new one. See `CHAT_IMPLEMENTATION_REPORT.md` §2/§9.
7. **No memory can ever be corrected or removed once created.** Combined with auto-capture's plain substring-match trigger (no NLP/intent understanding — see §3), a message that innocently contains a trigger phrase for an unrelated reason ("is our office open Friday?") creates a permanent, unremovable memory row. Needs an update/delete endpoint backend-side; this frontend deliberately does not fake one. See `MEMORY_IMPLEMENTATION_REPORT.md` §2/§10.
8. **A knowledge↔worker permission grant can be duplicated, and "Remove" can silently fail to fully revoke access.** `POST /permissions/` has no dedup check (see §3); the only delete path removes one row at a time. This app's own Permissions UI prevents creating the duplicate through normal use, but cannot prevent it via a direct API call, and cannot guarantee one "Remove" click is complete if a duplicate already exists. Needs a backend-side fix — a dedup check on create (mirroring the already-correct `assign_knowledge()`), a unique DB constraint on `(worker_id, knowledge_id)`, or both. See `ADMIN_IMPLEMENTATION_REPORT.md` §2/§8/§10.
9. **No user-role-change, deactivation, or delete endpoint exists.** A mis-assigned admin role, or a user who should lose access, cannot be corrected from this app. See `ADMIN_IMPLEMENTATION_REPORT.md` §10.
10. **No password policy is enforced backend-side**, and `role` has no server-side enum — any string passes `UserCreate`'s validation; only this frontend's own form imposes a non-empty-password and admin/member convention. See `ADMIN_IMPLEMENTATION_REPORT.md` §2.
11. **Connectors are entirely non-functional and not organisation-scoped even in principle.** Every organisation and every user sees the identical hardcoded connector status; the connector class hierarchy that would implement real OAuth is unreachable dead code (see §3). Not a frontend gap — this frontend accurately represents that reality rather than building around it. See `CONNECTORS_IMPLEMENTATION_REPORT.md` §2/§9.
12. **Every screen in Package 9 (Billing & Licensing) except real usage counts and the real organisation identity shown in one wizard step is illustrative reference data, not a real licensing record.** This is the largest, most consequential gap in the entire product — nothing about tier, hosting model, licence status, expiry, or entitlement requests is real or persisted anywhere. See `BILLING_AND_LICENSING_IMPLEMENTATION_REPORT.md` §3 for the full, itemised backend-requirements list needed to change this, starting with an organisation-level licence record (the single prerequisite everything else in that list depends on). **Partially updated, Package Q:** `WorkerPackWizard.js`'s submission is now real (it needed no reference-licence data at all — only `pack_size`/`quantity`); `RenewalWizard.js`/`OwnershipTransferWizard.js` and the rest of this section remain illustrative, since they need a real `tier`/`hosting_model`/`existing_licence_id` this package did not build. See ADR-021.
13. **No memory row of any tier (worker, department, organisation) or any memory summary can ever be corrected or removed.** Package H extended the memory hierarchy (departments, department memory, organisation memory, long-term summaries) without adding an update/delete endpoint anywhere — the same standing gap risk #7 already flagged for worker memory, now present at every tier rather than compounded in severity at any one of them. See `PHASE_0_PACKAGE_H_MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md` §11 / `MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md` §10.
14. **No user↔department or user↔worker membership model exists.** Department memory's read gate is "any member of the owning organisation," not "any member actually assigned to that department" — there is no data model anywhere for the latter. See `PHASE_0_PACKAGE_H_MEMORY_INTELLIGENCE_IMPLEMENTATION_REPORT.md` §1.
15. ~~Department is not yet a CTO delegation-routing signal.~~ **Resolved by Package I** — `_pick_worker_for_subtask()` now routes to a department's head when the department's own name/description best matches a subtask. Left here, struck through, per this file's own append-only convention for a risk that's since been closed rather than deleted outright.
16. **The department-routing heuristic is a plain keyword match on a department's name/description, same limitation as the pre-existing per-worker heuristic.** A department with a vague or generic description may not route correctly to its head — deterministic and testable by design, not a model, per this series' own scope discipline; see `PHASE_0_PACKAGE_I_DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md` §5.
17. **No user↔department-head relationship exists at the human level.** Headship is a Worker-level designation (which AI persona represents a department); there is no concept of a human user "being" or "reporting to" a department head — see `PHASE_0_PACKAGE_I_DEPARTMENT_HEAD_IMPLEMENTATION_REPORT.md` §1.
18. **A layout that conditionally renders instead of `{children}` does not stop the hidden child route's own Server Component (and its data fetches) from executing** — found and fixed for Package 9's own three new pages during smoke testing, but very likely still true of Package 7's pre-existing `/portal/admin/{users,organisation,permissions}` pages, not yet fixed there. The visible page has never been wrong in either case; only the raw, unrendered HTTP response bytes carry the extra (own-organisation-scoped, not cross-tenant) data. See `BILLING_AND_LICENSING_IMPLEMENTATION_REPORT.md` §9.
19. **No proposal, quote, contract, or onboarding task can ever be corrected or removed once created** (Package J) — the same standing "create and read/decide only" gap already flagged for every memory tier, now present on the sales/customer-success data model too. See `PHASE_0_PACKAGE_J_SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md` §10.
20. **`CrmContact` has no department-scoping** (Package J) — pipeline/customer-health data is organisation-wide; a "sales" and a "customer_success" department in the same organisation see the same underlying data, just presented differently, not genuinely separate per-department pipelines. Deliberate simplicity, not an oversight. See `SALES_AND_CUSTOMER_SUCCESS_IMPLEMENTATION_REPORT.md` §11.
21. **`Department.function` is not wired into CTO Orchestration's delegation routing** (Package J) — it identifies a department for dashboard purposes only; using it as an additional delegation-routing signal alongside the existing name/description heuristic (Package I) is a candidate for later, not built here.
22. **No update or delete endpoint exists for any campaign, content piece, video asset, or Media Centre item** (Package K) — the same standing "create and read/decide only" gap, now on the marketing/media data model too. See `PHASE_0_PACKAGE_K_MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md` §10.
23. **`Campaign`/`ContentPiece`/`VideoAsset`/`MediaCentreItem` have no department-scoping** (Package K) — organisation-wide, same trade-off `CrmContact` already made. See `MARKETING_AND_MEDIA_IMPLEMENTATION_REPORT.md` §11.
24. **`Department.function = "marketing"` is not wired into CTO Orchestration's delegation routing** (Package K) — same standing gap already flagged for `"sales"`/`"customer_success"` (item 21).
25. **No update or delete endpoint exists for any federation consultation** (Package L) — the same standing "create and read only" gap, now on a fifth data model. See `PHASE_0_PACKAGE_L_FEDERATION_AND_EXTERNAL_AI_CONSULTING_IMPLEMENTATION_REPORT.md` §10.
26. **`FederationProvider`'s registry is a small, fixed, hand-authored set (3 rows), not customer-manageable** (Package L) — no admin CRUD exists to add/edit/remove a provider, consistent with the scale of Packages 8/J/K's own hardcoded connector sets, but worth flagging explicitly since "Registry" in the name could otherwise imply a manageable catalogue.
27. **Federation's estimated tokens/cost are a simulated foundation, not real metering** (Package L) — there is no real external billing to measure since no real external provider call exists; never mistake `FederationConsultation.estimated_cost` for actual spend.
28. **No update or delete endpoint exists for any department budget** (Package M) — the same standing "create and read/decide only" gap, now on a sixth data model. See `PHASE_0_PACKAGE_M_CFO_AND_FINANCE_PLATFORM_IMPLEMENTATION_REPORT.md` §10.
29. **`Department.function = "finance"` is not wired into CTO Orchestration's delegation routing** (Package M) — same standing gap already flagged for `"sales"`/`"customer_success"`/`"marketing"` (items 21/24).
30. **No `Subscription` entity, invoicing, or commercial billing exists anywhere in this backend** (confirmed still true, Package M) — `services/finance_summary_service.py`'s `licensing` section surfaces real `Licence`/`Entitlement` data, but genuinely has no subscription-cost data to show; this is an honest gap, stated explicitly rather than left implicit. See §3's correction above.
31. **No update or delete endpoint exists for any project or task beyond status transitions** (Package N) — the same standing "create and read/decide only" gap, now on a seventh and eighth data model. See `PHASE_0_PACKAGE_N_OPERATIONS_AND_PROJECT_DELIVERY_IMPLEMENTATION_REPORT.md` §10.
32. **`Department.function = "operations"` is not wired into CTO Orchestration's delegation routing** (Package N) — same standing gap already flagged for `"sales"`/`"customer_success"`/`"marketing"`/`"finance"` (items 21/24/29), now affecting five values.
33. **No update or delete endpoint exists for any support request or message beyond status transitions and appending new messages** (Package O) — the same standing "create and read/decide only" gap, now on a ninth/tenth data model. See `PHASE_0_PACKAGE_O_CUSTOMER_PORTAL_AND_SELF_SERVICE_IMPLEMENTATION_REPORT.md` §7.
34. ~~No self-service signup, password reset, or email-based invite exists for `PortalContact` accounts.~~ **Password reset resolved by Package CX1** — a real `PortalContactPasswordResetToken` mechanism and `/portal-contact/{forgot-password,reset-password}` endpoints exist, live-verified end to end. Self-service *signup*/invite still doesn't exist — an admin still creates every account directly. Email *delivery* of the reset token is real as of Package EMAIL1 mechanism-wise, but no real SMTP credential is configured in this environment yet (see item 44's own update).
35. **`PortalContact` is 1:1 with a `CrmContact`, not a multi-user-per-customer model** (Package O) — exactly one login per contact; a customer organisation with multiple staff who need portal access is not supported without sharing one login.
36. ~~`Department.function` is not wired into CTO Orchestration's delegation routing.~~ **Resolved by Package PQR.** `_pick_worker_for_subtask()` now consults `department.function` as a static keyword-boost signal, unioned into the department's own scored words. Left here, struck through, per this file's own append-only convention for a risk that's since been closed rather than deleted outright.
37. **No update or delete endpoint exists for any worker creation request, deployment record, or platform incident beyond status/decide transitions** (Package PQR) — the same standing "create and read/decide only" gap, now on an eleventh, twelfth, and thirteenth data model. See `PHASE_0_PACKAGE_PQR_WORKER_GOVERNANCE_ARCHITECTURE_AND_PRODUCTION_OPERATIONS_IMPLEMENTATION_REPORT.md` §7.
38. **The governance policy registry does not retrofit the 24 pre-existing `require_role("admin")` call sites** (Package PQR) — a deliberate, stated scope boundary (ADR-020), not an oversight; those call sites keep their own proven enforcement code path, and the registry is authoritative only for this package's own new endpoints and for read visibility.
39. **`services/production_platform_health_service.py`'s health summary is a computed snapshot from real rows, not a real metrics/alerting/paging pipeline** (Package PQR) — no such infrastructure exists anywhere in this backend; honestly scoped to what real data (incidents, deployments) actually supports.
40. **Worker-limit entitlement enforcement is real for `POST /worker-pack-provisioning/` only** (Package Q) — the pre-existing `POST /workers/` (direct admin creation) and `WorkerCreationRequest` approval remain unenforced. `LICENSING_MODEL_V1.md` §4's "hard stop enforced at creation time" policy is therefore still not fully true of the product as a whole, only of its one newest path. See `PHASE_0_PACKAGE_Q_COMMERCIAL_PROVISIONING_AND_ENTITLEMENTS_IMPLEMENTATION_REPORT.md` §8.
41. **No update or delete endpoint exists for `OrganisationOnboardingTask` beyond marking one complete** (Package Q) — the same standing "create and read/complete only" gap, now on a fourteenth data model.
42. **`WorkerPackAddOn` has no maximum-stackable-packs cap** (Package Q) — `LICENSING_MODEL_V1.md` §7 itself lists this as not decided; implemented as an unbounded accumulator, consistent with that open question remaining open, not a decision to leave it unbounded forever.
43. **`RenewalWizard.js`/`OwnershipTransferWizard.js` remain illustrative-only** (Package Q) — only `WorkerPackWizard.js`'s submission was wired to a real backend endpoint, since its payload needed no data from `lib/licensing/referenceLicence.js`; the other two wizards need a real tier/hosting/licence-id source this package did not build. See §5 item 12's update above.
44. ~~Password reset has a real, tested token mechanism but no email delivery.~~ **Mechanism-wise done, email content now correct, wire-protocol correctness proven, credential still missing — Package EMAIL1 / Wave 1 Workstreams 2-3.** A real, generic `SmtpEmailProvider` exists (`services/email_provider.py`, works against a real Microsoft 365 mailbox via SMTP AUTH), with its wire-level correctness now proven against a real local socket (Workstream 2). Separately, and more fundamentally: the reset email's *content* was itself broken independent of any credential question — `request_password_reset()`/`request_portal_contact_password_reset()` generated a real raw token and then discarded it without ever passing it to the notification layer, so the email always read "use the link your administrator provides" with no link ever present, regardless of whether SMTP was configured. Both service functions now build a real `FRONTEND_BASE_URL`-based reset URL (a new backend config setting — none existed before) and thread it through `notify_password_reset_requested()` into a rewritten `_password_reset_requested` template that renders an actual working link (Workstream 3). The frontend's `/portal/reset-password` and `/customer-portal/reset-password` pages already correctly consumed a `?token=` parameter and needed no change. This environment still has no real SMTP credentials configured, so every notification (including this one) still falls back to the honest logging behaviour — see `docs/operations/PRODUCTION_RUNBOOK.md` §9 for the exact activation steps, now the only remaining step.
45. ~~Nothing enforces `Organisation.trial_ends_at`.~~ **Resolved by Package CX1.** A real grace-period-then-Locked-Mode state machine (`services/trial_service.py`) now blocks new worker creation and caps trial organisations at a worker limit; Package EMAIL1 added a proactive "trial ending soon" notice ahead of expiry. See ADR-022/ADR-024.
46. **No GPU metrics exist** (Platform Review Wave 1) — `services/system_resource_metrics_service.py` always reports GPU as `not_configured`; the objective itself named this "Future GPU support."

## 6. How to keep this file current

Any Developer Worker, QA Worker, or human completing a package from [[roadmap]] must update the table in §2 (or §3 for backend changes reported back from that repo) in the same change that ships the package. This file is read first by every other worker type before they act — stale rows here propagate bad assumptions everywhere. See [[worker-operating-standards]] §Memory and State Hygiene.
