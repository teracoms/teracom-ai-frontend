# dependencies
/node_modules

# next.js build output
/.next/
/out/

# local env files
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log

# typescript (tsconfig.json exists for editor tooling only — this repo is JS)
*.tsbuildinfo
next-env.d.ts

# VOICE_ACCESS_INVESTIGATION_V1 -- self-signed TLS cert/key are
# host-specific and the key must never be committed; regenerate per
# host with deploy/tls/generate-cert.sh
deploy/tls/voice-lan.key
deploy/tls/voice-lan.crt
